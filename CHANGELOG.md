# Changelog

Registro das alterações relevantes do ContaBase.

Este arquivo organiza as mudanças por versão e por tipo:

* **Adicionado**: novos recursos.
* **Alterado**: mudanças em funcionalidades existentes.
* **Corrigido**: correções de bugs.
* **Removido**: funcionalidades ou arquivos removidos.
* **Segurança**: mudanças relacionadas à segurança.
* **Limitações conhecidas**: pontos ainda não resolvidos ou planejados para versões futuras.

## [v0.1.0-beta.2]

### Adicionado
- **Busca de categorias sem acento:** em Configurações > Categorias, o campo de busca agora ignora acentos e diacríticos via normalização Unicode (NFD), permitindo encontrar "Saúde" ao digitar `saude`, "Educação" com `educacao` e "Cartão" com `cartao`. Acentuação original preservada na exibição. A mesma normalização foi aplicada ao combobox de seleção de categoria pai.
- **Testes de regressão para edição de séries:** 13 testes cobrindo alteração de data de lançamentos recorrentes nos escopos single, future e all, em faturas de cartão e contas corrente, para frente e para trás, com limite de ocorrências e garantia de fatura paga bloqueada.
- **Testes de regressão para limite disponível de cartão:** 14 testes cobrindo cálculo de limite usado/disponível com compras únicas, parcelamentos, recorrências, faturas OPEN/CLOSED/PAID, isolamento por workspace e cartão, exclusão de pagamentos e transações sem invoice_id.
- **Estados derivados de fatura por ciclo e situação financeira,** com badges independentes como `Aberta`, `Fecha em X dias`, `Fechada`, `Vazia`, `Pendente`, `Parcial` e `Paga`.
- **Seletor manual de fatura no Form Lançamento** para compras no cartão, com opção automática por ciclo, escolha avançada e atalhos para fatura anterior/próxima quando existirem.
- **Alerta visual para correção manual** em fatura fechada ou paga.
- **Identificação mais clara de cartões com nomes iguais no Form Lançamento,** exibindo subtítulo com informações disponíveis como emissor/bandeira e limite.

### Corrigido
- **Corrigida duplicidade ao editar lançamentos recorrentes com mudança de data.** Ao alterar a data de um lançamento recorrente nos escopos "somente este", "este e futuros" ou "toda a série", o sistema agora move corretamente as datas das ocorrências, recalcula as faturas vinculadas e preserva a projeção futura sem duplicar registros. Causas raiz: (1) o campo `date` estava ausente do UPDATE em `updateFutureRecurringTransactions`; (2) a projeção recorrente (`generateRecurrenceProjection`) era executada antes de atualizar as ocorrências existentes, usando a data antiga do banco como base; (3) o parâmetro `fromDate` usava a nova data em vez da data original. Correção cobre cartão de crédito e conta corrente. Regressão de lançamentos parcelados coberta por testes.
- **Corrigido cálculo de limite usado/disponível do cartão.** O limite em uso agora considera todas as despesas comprometidas em faturas abertas (OPEN) ou fechadas (CLOSED) ainda não pagas, incluindo parcelas de compras futuras e projeções de recorrentes materializadas em faturas. Faturas pagas (PAID) não consomem limite. Pagamentos de fatura não entram no consumo. Dashboard e página de Fatura usam a mesma regra canônica. Exemplo: uma compra parcelada de R$ 10.000,00 em 10x em um cartão com limite de R$ 10.000,00 passa a mostrar limite disponível R$ 0,00 (antes mostrava R$ 9.000,00 incorretamente, considerando apenas a parcela da fatura atual).
- **Corrigido cálculo de limite de cartão em faturas com pagamento parcial.**
- **Corrigido caso em que fatura quitada antecipadamente era pulada** e lançamentos do mesmo ciclo caíam indevidamente na próxima fatura.
- **Corrigido travamento/deadlock no endpoint `/cartoes/fatura-destino/...`** ao carregar opções com `include_options=true`.
- **Corrigido loop/flicker das ações rápidas de fatura no Form Lançamento.**
- **Corrigida ordenação do seletor avançado de fatura** para exibir faturas em ordem cronológica crescente.
- **Corrigida exibição de quick actions** para impedir seleção de faturas inexistentes ou sem `invoice_id` válido.
- **Corrigido estado transiente** em que quick actions podiam mostrar o mês automático como opção de mover.
- **Corrigido cache de opções de fatura ao trocar de cartão** no Form Lançamento.
- **Corrigido badge financeiro `Parcial`,** que aparecia como `Pendente` em faturas parcialmente pagas.
- **Corrigido teste de badge de ciclo** que dependia de horário/timezone.
- **Corrigido layout do Form Lançamento ao ativar parcelamento,** evitando corte de conteúdo, sobreposição do botão de confirmação e grande vazio inferior.

### Alterado
- **Redesign visual do financeiro denso (Dense Financial UI):** aplicado padrão visual com cards densos, chips, barras de progresso, linhas compactas e tokens visuais nas principais telas incluindo Dashboard, Metas, Lançamentos, Faturas, Relatórios, Contas, Contatos, Ajuda e Configurações. O Dashboard foi refinado com cards de cartões mais visuais, hierarquia de saldo melhorada e barras de progresso para limites e metas. Configurações foi reorganizada visualmente, incluindo Perfil, Workspace, Categorias, Contas, Cartões, Auditoria e telas administrativas. Ajustes de legibilidade e responsividade no Dashboard e nas telas de faturas e cartões.
- **Scripts de atualização revisados:** scripts privados de deploy passaram a espelhar a branch remota com `git fetch` + `git reset --hard`, evitando falha de divergência. Scripts públicos preservam comportamento seguro com fast-forward ou `--force-reset` explícito. Melhorias na robustez dos fluxos de atualização sem alterar dados, `.env`, banco ou volumes.
- **Cálculo canônico de limite de cartão implementado:** criada função compartilhada `sumCardOutstandingLimit` que soma despesas EXPENSE em faturas OPEN/CLOSED isoladas por workspace e cartão. Dashboard e página de Fatura foram migrados para essa função, substituindo o cálculo anterior que considerava apenas uma fatura.
- **Limite usado/disponível do cartão** passou a ser calculado pelo saldo pendente das faturas, e não mais pelo total bruto das compras.
- **Pagamentos parciais** liberam limite proporcionalmente.
- **Quitação total de fatura** zera o consumo de limite daquela fatura.
- **Fatura paga deixou de ser bloqueio rígido** para correções manuais.
- **Fatura paga antecipadamente** antes do fechamento continua no ciclo atual e pode receber novos lançamentos automáticos do mesmo ciclo.
- **Roteamento automático de compras no cartão** passou a considerar o ciclo da fatura, não apenas o status `OPEN`.
- **O Form Lançamento** passou a usar `invoice_id` para seleção manual de fatura, mantendo `fatura_offset` como comportamento automático.
- **Seletor de fatura** passou a recarregar opções ao trocar de cartão, evitando opções antigas/estagnadas.
- **Formulário** passou a tratar parcelamento e despesa fixa/recorrente como opções mutuamente exclusivas na interface.
- **Quick actions de fatura** agora limpam opções antigas ao trocar de cartão e só aparecem após carregar opções compatíveis com o cartão atual.
- **O estado financeiro `Parcial`** passou a ser exibido quando a fatura possui pagamento ativo e ainda tem saldo pendente.
- **Refinado o visual dos cards da seção Meus cartões no Dashboard,** removendo o texto repetitivo `Cartão de Crédito`, melhorando hierarquia, espaçamentos, tipografia e mantendo a barra inferior de limite.
- **Cards de cartões compactados no mobile,** com `Fatura atual` e `Vencimento` em layout inline para reduzir altura sem perder legibilidade.
- **Fundo sutil tom sobre tom/glass** adicionado aos cards de cartões para melhor separação visual em light/dark theme.
- **Layout responsivo do Dashboard refinado** para manter a ordem mobile: `Posição financeira`, `Minhas contas`, `Meus cartões`, `Limites`, `Metas & Reservas` e demais blocos.
- **Alinhamento desktop da linha superior do Dashboard ajustado,** alinhando `Posição financeira` com os cards à direita.
- **Atalhos Extrato, Obrigações, Reservas e Fechamento** ocultados no mobile, mantendo-os no desktop.
- **Legibilidade mobile dos valores secundários melhorada** para `Reservado` e `Disponível` no card de posição financeira.
- **Página `/notificacoes` alinhada ao Design System atual,** com cards de notificação visualmente mais consistentes, bordas/severidades suavizadas e melhor consistência em dark/light theme, desktop e mobile.
- **Ações Aportar e Liberar em Reservas** migradas de controles inline pequenos para modal/bottom sheet com campo monetário maior, contexto da reserva e botões claros.
- **Botões primários dos sheets simplificados** para `Confirmar`.
- **Erros de aporte/liberação** passaram a aparecer dentro do sheet, preservando a experiência do usuário.
- **Resumos de Reservas e Limites** redesenhados como painéis compactos de estatísticas, diferenciando-os dos cards da lista.
- **Compatibilidade visual com light/dark theme** preservada nos sheets e painéis de Reservas e Limites.

### Validação
- Testes permanentes para estados de fatura, limite por saldo pendente, pagamento parcial, quitação, fatura paga não bloqueante, roteamento por ciclo e fatura paga antecipadamente.
- Smoke financeiro validou pagamento parcial seguido de quitação total, reabertura de fatura paga, ausência de 409 indevido e limite usado/disponível coerente.
- Smoke visual validou Form Lançamento, quick actions, cards do Dashboard, Notificações, Metas/Reservas, light/dark theme, desktop e mobile.
- Validações executadas ao longo das fases:

  - `git diff --check`
  - `bash scripts/build-css.sh`
  - `go test ./...`
  - `go vet ./...`
  - `go build ./...`
  - `node --check assets/js/form-lancamento.js`

## [0.1.0-beta.1] - 2026-06-22

*Release Beta preparada localmente. A tag e a publicação remotas ainda não foram executadas.*

### Adicionado
- **Instalação por release artifact:** adicionados o dispatcher `scripts/install.sh`, o instalador `scripts/install-contabase-release.sh` e o workflow que gera bundles Linux `amd64`/`arm64` com `checksums.txt`. A instalação valida SHA-256 e estrutura do tarball antes de alterar o sistema.
- **Documentação pública da Beta:** preparados README, índice de release notes, nota completa de `v0.1.0-beta.1`, guias de instalação/atualização e URLs públicas dos assets esperados. A release e os assets ainda não foram publicados.
- **Canal Docker GHCR:** preparado workflow multi-arch para publicar as tags fixas da versão e o canal mutável `beta`, sem `latest`, quando houver tag pública ou opt-in manual explícito.

### Alterado
- **Trilhas de instalação:** separados os caminhos Docker Compose, release artifact e source/build local. Docker continua recomendado para teste rápido e isolamento; release artifact é o caminho indicado para VPS/LXC Debian sem Docker; source permanece avançado.
- **Update Docker seguro:** o updater passou a fazer backup com a stack parada, reler a versão após o pull, validar healthcheck e tentar rollback do runtime em falha, sem restore automático de dados.
- **Deploy binario/systemd (P.29.d.2):** corrigida a instalacao binaria/systemd em Debian 12 descartavel. Causa raiz: o `apt` do Debian 12 entrega Node 18, e nesse caso o `npm` pula o binario nativo `@tailwindcss/oxide` (Tailwind 4 exige Node 20+), quebrando o `build-css.sh` com `Cannot find native binding` / `@tailwindcss/oxide-linux-arm64-gnu` ausente. `scripts/install-contabase-source.sh`, `scripts/update-contabase-source.sh` e `scripts/build-css.sh` passaram a exigir Node 20+ no preflight e abortam com mensagem clara em vez de falhar de forma obscura. A deteccao de PID 1 systemd deixou de depender de `ps`/`procps` e passou a ler `/proc/1/comm`. O healthcheck do instalador agora usa retry (30 tentativas) antes de declarar falha. `docs/instalacao-lxc-vps.md` e `docs/binario/README.md` documentam o requisito de Node 20+ via NodeSource e a necessidade de `chmod +x`. Nenhuma alteracao em auth, RBAC, workspace, banco, calculo financeiro ou no fluxo de Docker. `VERSION` nao foi alterada.
- **Deploy binario/systemd (P.29.d.2b):** alinhado o requisito de Go na trilha binaria. A documentacao deixava de funcionar no Debian 12 porque sugeria `golang-go` do apt (Go 1.19), incompativel com o `go.mod` (Go 1.26.3). `scripts/install-contabase-source.sh` e `scripts/update-contabase-source.sh` passaram a validar no preflight (antes de qualquer alteracao de servico) se a versao do Go instalada e maior ou igual a exigida pelo `go.mod`, abortando com mensagem clara caso contrario. Sem download automatico de toolchain. `docs/instalacao-lxc-vps.md` e `docs/binario/README.md` passaram a indicar Go via tarball oficial/gerenciador externo e marcam `golang-go` do apt como inadequado no Debian 12. Nenhuma alteracao em auth, RBAC, workspace, banco, calculo financeiro ou no fluxo de Docker. `VERSION` nao foi alterada.
- **Deploy Docker (P.29.d.3):** corrigido o falso warning de healthcheck no `scripts/update-contabase-docker.sh` quando o ContaBase publica uma porta customizada no host (ex.: `18080:8080`). O script passou a resolver a porta publicada via `docker compose port contabase <PORT>`, lendo o `PORT` (padrao 8080) de `.env.docker`, e usa a porta efetiva no host ao chamar `/health`. Fall-back para `8080` se a resolucao falhar. Comportamento de backup e rollback inalterados. `docs/atualizacao.md` atualizada com nota sobre porta customizada e resolucao automatica. Nenhuma alteracao em auth, RBAC, workspace, banco, calculo financeiro ou na trilha binaria/systemd. `VERSION` nao foi alterada.
- **Autenticacao (P.29.d.1/P.29.d.1b):** concluido o hardening dos recovery codes do 2FA com formato Base32 canonico de 8 caracteres, compatibilidade com codigos legados de 7 caracteres e consumo atomico compare-and-swap. O codigo exibido corresponde ao hash persistido, reutilizacao e consumo concorrente duplicado sao rejeitados e falhas de persistencia permanecem fail-closed. Nenhum outro aspecto de auth, RBAC, workspace, sessao ou dados financeiros foi alterado.
- **Operacao pre-Beta (P.29.e/P.29.f):** validados em ambientes descartaveis login, logout, TOTP, recovery codes, RBAC, instalacao Docker limpa em Linux, instalacao/update/rollback binario com systemd em Debian 12, update/rollback Docker, backup e restore completos. Os dados ficticios de usuario, workspace, contas, cartoes/faturas, lancamentos, metas/reservas, configuracoes e uploads permaneceram consistentes apos o restore.
- **Visual pre-Beta (P.28.e.fix/P.29.f/P.30.a):** aprovados os ajustes de affordance horizontal da DRE em mobile, legibilidade da aba Transferir, posicionamento do FAB e hierarquia visual da tela de fatura. O bloco de limite do cartao permanece secundario em relacao ao total, pago e pendente, sem alteracao de calculo financeiro ou regra de fatura.
- **Documentacao de release (P.30.a/P.31.a):** reconciliados VERSION, CHANGELOG e checklist interno para a preparacao local de `v0.1.0-beta.1`, mantendo B4 e B5 como ressalvas nao bloqueantes. Nenhuma tag, promocao, publicacao ou release foi executada nesta fase.
- **Ajuda (P.22):** concluida a otimizacao da pagina `/ajuda`, com indice de busca pre-computado para reduzir trabalho repetido no render e teste de contrato de render adicionado. Mantidos intactos conteudo, rotas, RBAC, JS/CSS e contratos HTMX.
- **Configuracoes (P.24):** realizado smoke manual de `/configuracoes` e subsecoes (shell, admin, auditoria, RBAC, workspace business-only), confirmando o comportamento apos a otimizacao P.23. Validacao documental/operacional; nenhuma alteracao de codigo, template, JS, CSS, rotas ou calculo.
- **Auditoria critica (P.26):** realizada auditoria critica de leitura em Dashboard, Relatorios e Faturas, confirmando coerencia de saldo, competencia, conciliacao de faturas/pagamentos e isolamento por workspace. Auditoria documental, sem alteracao de SQL, calculo financeiro, handlers, ViewModel, schema, rotas, templates, JS ou CSS.
- **Lifecycle HTMX/assets (P.25):** documentado internamente o lifecycle global de scripts, eventos HTMX/DOM, FAB, overlays, workspace/context e contratos de swap/OOB. Realizado smoke manual em `/lancamentos`, `/contas`, `/metas`, `/contatos`, `/ajuda`, `/configuracoes` e `/admin/auditoria`, sem detectar duplicacao visivel de listeners, FAB, icones, overlays ou workspace/context. Nenhuma alteracao de JS/CSS/templates/handlers foi realizada.
- **Configuracoes (P.23):** concluida a otimizacao da area `/configuracoes`, com testes de contrato para shell, subsecoes, HTMX, RBAC, admin, auditoria e workspace business-only. O assembly de leitura foi separado entre defaults, shell completo e dados por secao, reduzindo consultas em partials HTMX e respostas pos-mutation sem alterar comportamento publico. A auditoria passou a separar render completo e fragmento `auditoria-table-body`, evitando carregamentos desnecessarios no filtro de linhas. Tambem foi removido `HandleConfiguracoesPage` como codigo morto confirmado. Mantidos intactos auth, RBAC, sessao, 2FA, backup, escrita de auditoria, rotas, templates, JS, CSS e contratos HTMX.
- **Contatos (P.21):** concluida a otimizacao da pagina `/contatos`. O titulo foi alinhado para "Contatos"; os partials de formulario e resumo foram reutilizados para reduzir duplicacao de template; a exclusao foi estabilizada para evitar duplo DELETE e garantir remocao visual imediata com HTMX; e o caminho de leitura foi separado entre pagina completa e lista lean, reduzindo `partial=lista`, busca, tabs e refresh pos-criacao para apenas a consulta de contatos. Mantidos intactos CRUD, permissoes/RBAC, workspace/tenant isolation, `custom_client_id`, rotas, JS/CSS e contratos de FAB/overlay.
- **Metas (P.20):** concluida a otimizacao da pagina `/metas`. O render passou a coalescer metadata de usuario/workspace, reduzindo o caminho quente de 8 para 5 queries no render normal. Cards de limites e caixinhas tiveram partials extraidos para reduzir duplicacao de template. A emissao do FAB/OOB foi normalizada para evitar duplicacao em respostas HTMX de `/metas`. Mantidos intactos os calculos de reserva, limite, caixinha, projecoes, HTMX, OOBs de formulario, auth/RBAC e workspace.
- **Categorias (bloco de correcoes):** fechada a trilha que corrigiu a hierarquia visual por `parent_id`, a validacao de `parent_id` por workspace/tipo/grupo macro/raiz, a normalizacao dos `macro_group` canonicos, o seed canonico Pessoal/Business e a rotina segura de `category-reseed` por workspace. A tela de Configuracoes > Categorias passou a abrir com pais colapsados, busca local que expande o pai correspondente e filhos mais legiveis.
- **Deploy binario (operacional):** corrigida a colisao com a variavel `VERSION` do sistema em scripts de install/update binario. A precedencia passou a ser `CONTABASE_VERSION` -> arquivo `VERSION` -> `dev`, e o healthcheck de update agora usa retry antes de acionar rollback.
- **Performance (P.3):** instrumentada a performance de Relatorios e Metas com logs `[PERF]` quando `APP_DEBUG` esta ativo.
- **Performance (P.4):** reduzido payload do seletor de categorias removendo markup e atributos redundantes por item. Sem alteracao de regra de categoria.
- **Performance (P.5):** reduzido payload inicial de `/transacoes/nova` removendo markup oculto e variaveis JS mortas. Sem alteracao de persistencia.
- **Performance (P.6):** removido refresh redundante do resumo financeiro em `/lancamentos`. Resumo atualizado via OOB quando necessario.
- **Performance (P.7/P.7.1):** reduzidas inicializacoes duplicadas do HTMX lifecycle. Coalescing de callbacks afterSwap. Preservados initAppShell e lucide.
- **Performance (P.8):** evitado render OOB redundante no DELETE de `/lancamentos`. Sem alteracao de exclusao, saldo ou fatura.
- **Performance (P.9–P.12):** reducao de markup por linha em rows, filtros e seletor mensal. Swipe deriva URLs por `data-tx-id`. Classes repetidas de status e acoes movidas para CSS. Marcadores do picker de categorias simplificados.
- **Performance (P.13):** scripts inline do formulario de lancamento movidos para asset (`form-lancamento.js`). Inicializacao preservada via DOMContentLoaded e afterSwap.
- **Performance (P.14):** sugestoes preditivas do formulario filtram por tipo de lancamento. Corrigida particao por descricao/conta/categoria/destino. Evita vazamento de destino em despesa. Testes adicionados.
- **Performance (P.15):** seletor de categorias usa markup compacto e CSS estatico. Payload reduzido sem alteracao de regra.
- **Performance (P.16):** `/lancamentos` troca mes por `partial=lista` atualizando apenas `#lancamentos-list-wrapper`. Resumo e seletor atualizados via OOB. URL canonicada via `HX-Replace-Url`. Preservacao de mes entre `/lancamentos`, `/contas` e `/relatorios` via handler generico `data-preserve-month`. Correcoes no componente compartilhado `seletor_meses` para `/contas` e `/relatorios`.
- **Performance (P.17):** reducao de payload dos refreshes filtrados. Classes repetidas de status/actions de rows movidas para CSS. `HX-Replace-Url` remove `partial=lista` e `q` vazio da URL canonica.
- **Performance (P.18):** otimizacao de `/relatorios`. Desativadas 5-6 queries cujos campos nao sao referenciados no template atual (Membros, TopClientsByRevenue, uncategorized, pending totals); funcoes, structs e campos preservados para uso futuro. Padroes repetitivos de KPIs, categorias e DRE movidos para `relatorios.css`. Troca mensal passou a usar partial rendering em `#relatorios-body` com `HX-Replace-Url`, reduzindo payload de troca de mes. Hard reload e navegacao entre paginas continuam full render. Sem alteracao de SQL financeiro, DRE, saldo acumulado, categorias, competencia, politica hibrida, schema, workspace, RBAC ou auth.
- **Performance (P.19):** otimizacao de `/contas`. `partial=lista` passou a executar somente a query da aba ativa (7 → 1 query em tabs, busca e refresh de lista). Troca de periodo e `refreshFinancials` atualizam `#contas-body` com `partial=content` (3 queries, sem layout), mantendo summary cards e lista sincronizados. `HX-Replace-Url` remove `partial` da URL canonica. Corrigida duplicacao de `periodo`/`partial` causada por heranca HTMX (`hx-disinherit="*"`). Removidos handler morto (`HandleListar`), template morto (`contas-listas`, 195 linhas) e CSS redundante. Cards de A Pagar e A Receber deduplicados no template ativo via variavel `$isPagar`. Sem alteracao de SQL financeiro, saldo, faturas, pagamentos, vencimento, status, schema, workspace, RBAC ou auth.
- **Dashboard (P.27):** atualizado visualmente o card financeiro, agora exibido como "Posição financeira", com Saldo real em destaque principal e Saldo das reservas/Disponível como métricas secundárias. O card recebeu destaque visual sutil e manteve valores negativos explícitos. Nenhum cálculo, SQL, handler, ViewModel ou JS foi alterado.
- **Faturas (P.28):** adicionada visualização de limite do cartão na tela de fatura, exibindo limite total, valor em uso na fatura, disponível estimado e barra de percentual disponível. A implementação reutiliza a semântica do Dashboard, sem alterar pagamento, quitação, invoice_payments, rotas, Dashboard, JS/CSS separados ou cálculo financeiro ativo.
- **Faturas (ed955bd):** ajustado o bloco de limite do cartão dentro da fatura para reduzir a leitura como valor principal da tela, deixando total da fatura, total pago e pendente como destaque dominante e mantendo limite em uso/disponível como informação secundária. Nenhum cálculo, SQL, handler, ViewModel ou regra de fatura foi alterado.

### Corrigido
- **Limites:** corrigida a validacao de Limites para impedir limite duplicado por categoria no mesmo workspace, exibindo erro amigavel.
- **Contas (P.19.d):** corrigido refresh visual apos editar lancamento dentro de `/contas`. O endpoint de save retornava fragmento `lancamento-row` (formato de `/lancamentos`) que era inserido nos cards de `/contas`, quebrando o layout. Agora detecta origem via `HX-Current-URL` e dispara `refreshFinancials` para atualizar `#contas-body` com template correto. Sem regressao em `/lancamentos`.
- **Lancamentos (P.17.e — filtros + navegacao mensal):** corrigida a perda de filtros ativos ao trocar mes em `/lancamentos`. Causa raiz: o seletor mensal OOB era renderizado sem filtros no query string dos botoes, descartando `situacao`, `tipo`, `origem`, `destino`, `categoria`, `q` e `ordem`. Tentativas client-side P.17.b/c/d (injecao de filtros via `htmx:configRequest`) foram descartadas por divergencia de estado entre URL, hidden inputs e seletor OOB. Correcao server-side: criado helper `lancamentosMonthQueryWithFilters` que preserva filtros multi-value nos URLs do seletor; hidden `mes`/`ano` do formulario sincronizados por OOB minimo (`lancamentos-filter-period`); removidos handlers especificos de `htmx:configRequest` para filtros e mes; mantido apenas handler generico `data-preserve-month`. Sem alteracao de SQL financeiro, competencia, saldo, fatura/pagamento, recorrencia/parcelamento, schema/migration ou workspace/RBAC/auth.

## Histórico de desenvolvimento incorporado à v0.1.0-beta.1

*Alterações validadas durante a preparação da primeira Beta pública controlada.*

### Adicionado
- **Faturas (Pagamento Parcial):** Adicionado suporte a pagamento parcial de faturas de cartão de crédito. Agora é possível pagar valores parciais de faturas abertas ou fechadas, registrando pagamentos parciais no histórico `invoice_payments` e debitando a conta de pagamento correspondente.
- **Faturas (Visual):** Adicionado modal de pagamento parcial dinâmico e responsivo na tela de detalhes de fatura, apresentando em tempo real o cálculo do saldo restante antes da confirmação.
- **Testes:** Criado teste de integração robusto `TestDashboardMonthlyCashSummaryAndCompetenceLimits` em `internal/handlers/dashboard_monthly_limits_test.go` para validar o resumo de caixa mensal do Dashboard, os limites por competência e a exclusão correta de faturas/pagamentos.
- **Testes:** Adicionado teste de conciliação de faturas deletadas `TestRelatoriosInvoicePaymentDeletedTransactions` em `internal/handlers/relatorios_invoice_payment_test.go` garantindo que registros fisicamente excluídos não entrem nos cálculos de competência de relatórios.
- **Testes:** Adicionados testes de conciliação estrutural de faturas (`TestExcludeInvoicePaymentCompetenceClauseUsesStructuralLink` em `internal/handlers/invoice_payment_test.go` e `TestRelatoriosCompetenciaExcluiPagamentoFaturaDescricaoAlterada` em `internal/handlers/relatorios_invoice_payment_test.go`) para garantir o funcionamento correto com nomes/descrições alterados pelo usuário.
- **Relatórios:** Adicionado quarto card "Saldo Acumulado" no topo de `/relatorios`, reutilizando a mesma regra financeira já existente em `/lancamentos` (`projectedAccumulatedBalance`). A lógica foi extraída para funções package-level compartilhadas (`calcProjectedAccumulatedBalance`, `calcProjectedTransactionNetCashFlowBetween`, `calcProjectedRecurringNetCashFlowBetween`, `calcPaidNetCashFlowBetween`), sem duplicação de SQL e sem alteração do cálculo original.

### Alterado
- **Lançamentos (DS):** Alinhado background/base de `/lancamentos` com o padrão DS compartilhado usado em `/relatorios` e demais telas atualizadas.
- **Lançamentos (DS):** Rows de lançamentos e faturas em `/lancamentos` passaram a usar superfície neutra. Status continua indicado por ícones, valores e badges, não por fundo vermelho/amarelo dominante.
- **Categorias (DS):** Alinhados background, wrapper, formulários, rows, tabs e empty states de Configurações > Categorias com tokens e componentes DS atuais.
- **Form Lançamento (DS):** Tokenizados resíduos visuais não-`sheet-*` do Form Lançamento/FAB sheet. Sistema `sheet-form.css` (dependente de JS) mantido intacto para fase dedicada futura.
- **Form Lançamento (CSP):** Reduzido JavaScript inline do `form_lancamento.html`, extraindo bridges, helpers visuais e observabilidade para `window.ContaBaseFormLancamento` em `assets/js/form-lancamento.js`. (D.5.3.9)
- **Form Lançamento (CSP):** Nomeadas sources de `overlayDidOpen` em fluxos conhecidos do formulário, eliminando `source: "unknown"`. (D.5.3.9.c)
- **Form Lançamento (CSP):** Escopo dos inicializadores `form-lancamento.js` restrito por `event.detail.target`, reduzindo varreduras globais em swaps HTMX/OOB. (D.5.3.9.d)
- **Faturas (JS):** Extraídos os scripts inline da página de faturas para `assets/js/faturas.js`.
- **Faturas (CSP):** Mantidas as ações de modais de quitação e pagamento parcial por funções compatíveis com delegação via `data-onclick`.
- **Faturas (HTMX):** Adicionada inicialização idempotente e compatível com HTMX para os modais de fatura após swaps.
- **Faturas (Fluxo de Status):** Modificada a regra de transição de status para faturas pagas: faturas `OPEN` (Abertas) permanecem como `OPEN` mesmo quando pagas totalmente antecipadamente (continuando aptas a receber novos lançamentos até seu fechamento de ciclo), enquanto faturas `CLOSED` (Fechadas) mudam para `PAID` ao ter seu saldo pendente zerado.
- **Faturas (Segurança):** Adicionada trava otimista (*Optimistic Concurrency Control*) baseada no acumulado do saldo pago (`COALESCE(paid_amount, 0) = ?`) na query de atualização da fatura para impedir pagamentos duplicados ou maiores que o saldo pendente em requisições concorrentes.
- **Faturas (OOB / HTMX):** A atualização pós-pagamento agora é feita de forma totalmente dinâmica via HTMX, injetando o novo resumo e histórico de pagamentos na fatura e atualizando os saldos das contas e cards do Dashboard via swaps OOB (Out-of-Band) sem necessidade de reload.
- **Faturas/Cartões:** Removida a dependência de padrões textuais nas descrições (`LIKE 'Pagamento de Fatura - %'`) para identificar pagamentos de faturas nos relatórios e cockpit. A identificação agora é baseada na estrutura de dados do SQLite, verificando o relacionamento com registros ativos na tabela `invoice_payments` (usando `transaction_id`, `workspace_id` e `reversed_at IS NULL`), prevenindo erros caso o usuário altere a descrição do lançamento.
- **Dashboard/Relatórios/Notificações:** Atualizadas todas as queries que contabilizam e exibem receitas, despesas, médias e previsões de competência e caixa para utilizarem a nova cláusula de exclusão estrutural de pagamentos de faturas.
- **Dashboard (Layout):** Refinamento do layout de Grid e ordenação mobile do Dashboard usando as propriedades CSS `contents` e `order-*` do Tailwind. Garante que os cards de Caixa do mês, Previsão do mês e Próx. 7 dias apareçam logo no topo no mobile (imediatamente abaixo do Saldo disponível), enquanto preserva o comportamento de colunas independentes no desktop.
- **Dashboard (Ajuste Visual):** Equalização vertical dos 3 cards de cockpit do topo ("Caixa do mês", "Previsão do mês" e "Próx. 7 dias"). Títulos encurtados com suporte a truncamento no mobile e subtítulos padronizados para altura fixa de 2 linhas (`h-8`), forçando o início dos blocos de dados financeiros e saldo a ficarem alinhados na mesma linha horizontal.
- **Dashboard:** O painel "Resumo do Mês" agora reflete **caixa realizado**, contabilizando apenas entradas e saídas de transações marcadas como "pagas". Compras no cartão de crédito são ignoradas aqui (evitando dupla contagem), e faturas de cartão entram apenas pelos seus pagamentos reais.
- **Dashboard:** As listas de pendências "A Pagar" e "A Receber" agora exibem itens vencidos (atrasados) somados aos previstos para os próximos 7 dias.
- **Dashboard:** O totalizador "A Pagar" agora inclui faturas de cartão de crédito abertas/fechadas com saldo pendente (valor total menos amortizações em `invoice_payments`) sem gerar dupla contagem com compras individuais.
- **Dashboard/Relatórios:** Alinhada a política híbrida de conciliação financeira: fluxo de caixa para o Resumo do Mês (caixa realizado) e regime de competência por data de compra para o Relatório DRE e para os Limites de Gastos.
- **Limites de Gastos:** Os limites mensais por categoria agora são baseados em competência (data da compra) e passam a excluir transações de pagamentos de faturas de cartão de crédito (mesmo se categorizados), prevenindo duplicidade no consumo de limites.

### Corrigido
- **Form Lançamento (CSP):** Removidos bloqueadores pontuais de `unsafe-inline` em fallbacks, dead code, modais simples, caixinha e helpers visuais de `form_lancamento.html`. Core sensível (submit, categoria, origem, status_pagamento, recorrência) mantido inline por segurança. (D.5.3.9)
- **Faturas (CSP):** Removidos bloqueadores de `script-src 'unsafe-inline'` na página de faturas sem alterar regras financeiras, payloads ou handlers Go.
- **Recorrências (Refresh):** Corrigido o refresh pós-exclusão de recorrências e parcelamentos em série, garantindo atualização imediata da lista após sucesso do DELETE.
- **HTMX 2.x:** Removida dependência incorreta de `.then()` em `htmx.ajax()` no HTMX 2.x.
- **Recorrências:** Correção do status de recorrências não-cartão recém-criadas como Pago. A ocorrência atual fica como paga (`PAID`), mas as projeções futuras de recorrências não-cartão são criadas como pendentes (`PENDING`) por padrão, respeitando a regra do motor contábil (cartões de crédito continuam como exceção).
- **Recorrências:** O toggle de status (ação rápida pago/pendente) de um lançamento recorrente agora afeta unicamente a ocorrência selecionada, sem alterar o status padrão da série.
- **Recorrências:** Preservação do parâmetro `total_occurrences` da regra associada em edições amplas de série (`escopo=all`), e correção dos escopos de edição (`single`, `future`, `all`) e remoção física (delete) via HTMX/OOB.
- **Faturas (Visual):** Removidos os botões de ordenação duplicados e confusos ("Recentes" / "Cronologico") no bloco de resumo/meses de faturas. A ordenação é gerida apenas pelas abas no cabeçalho de lançamentos.
- **Faturas (HTMX/OOB):** A unificação do template de faturas sob `invoice-transactions` garante que todas as operações dinâmicas (excluir, mover fatura, editar e alterar status) atualizem as listas e resumos de forma limpa via out-of-band swaps, sem necessidade de F5.
- **Relatórios:** Correção em Gastos por Categoria de `/relatorios` para garantir que subcategorias não sumam da visualização quando houver lançamentos apenas em categorias filhas (preservando a árvore hierárquica completa).
- **Relatórios:** Implementada exclusão de pagamentos de faturas do regime de competência dos relatórios de forma totalmente estrutural (via `invoice_payments.transaction_id`), garantindo consistência mesmo com descrições customizadas.

### Removido
- **Lançamentos:** Removido `templates/components/transacao-detalhe.html`, template legado dark-only de edição inline de transação. Substituído por `form_lancamento.html` renderizado via `#bottom-sheet-container`.
- **Handlers:** Corrigido texto de log em `HandleTransacaoDetalhe`: "template detalhe error" → "template form-lancamento error" (refletia template errado).


### Corrigido
- **UI:** Ajustado o layout das tabs de contatos (`/contatos`) para dispositivos móveis: reduzida a fonte para `text-[11px] min-[375px]:text-xs`, o gap para `gap-1 sm:gap-2` e adicionada abreviação responsiva ("Fornec." no mobile < 375px e "Fornecedores" em telas maiores/desktop) para evitar que a aba de fornecedores seja espremida ou escape do container.
- **UI:** Corrigido o transbordamento e corte visual de valores monetários longos (ex: na casa dos milhões com sinal negativo) nos 4 cards de KPIs superiores em `/relatorios` no mobile. Adotado fallback seguro para grid de 1 coluna abaixo de 390px (`grid-cols-1 min-[390px]:grid-cols-2 sm:grid-cols-4 gap-3`), tipografia sintonizada (`text-[0.92rem] min-[390px]:text-base sm:text-xl xl:text-2xl`) com tracking reduzido (`tracking-[-0.03em]`), garantindo que nenhum valor financeiro seja truncado ou cortado por overflow.
- **UI:** Padronizado o cabeçalho contextual global em desktop e mobile: a barra de workspace exibe o nome da página atual à direita (ex: "Lançamentos", "Contas", "Relatórios") em ambos os breakpoints. O indicador `#page-indicator` (anteriormente `#mobile-page-indicator`) teve a classe `md:hidden` removida, unificando o comportamento. Cabeçalhos internos grandes redundantes removidos definitivamente de todas as páginas principais, sem HTML comentado residual.
- **UI:** Ajustado o refinamento visual da barra de seleção em lote de `/lancamentos` para tamanho legível (`text-xs`, checkbox `w-4 h-4`, padding `py-2`), mantendo o aspecto leve sem fundo forte, borda discreta (`border-zinc-200/25`, `rounded-lg`) e destaque lilás sutil apenas no modo ativo. Corrige o excesso da iteração anterior que havia reduzido demais o texto e elementos.
- **JS:** Corrigido o contador de itens selecionados em `/lancamentos` ao marcar/desmarcar checkboxes individuais. Causa raiz: em `bulk-select.js`, o handler `data-ignore-select` retornava antes do handler `data-tx-checkbox`, bloqueando o `toggleId()` e impedindo a atualização de `syncDOM()`. Movido o handler dos checkboxes para antes da guarda `data-ignore-select`.
- **UI:** Corrigida a regressão no cabeçalho mobile que impedia a atualização dinâmica do nome da página à direita do workspace atual durante navegações HTMX. A causa raiz era o elemento `#mobile-page-indicator` em `layout.html` estar envolto pela condicional `{{if .Title}}`, fazendo com que ele não fosse gerado no DOM caso a página inicial (como o Dashboard) não possuísse um título correspondente, impedindo atualizações futures via JavaScript. O wrapper condicional foi removido para garantir a presença permanente do elemento no DOM.
- **UI:** Ajustado o espaçamento inferior da linha de resumo compacto em `/lancamentos` (de `mb-3.5` para `mb-5`) para resolver a sobreposição visual com os seletores de lote e a lista de transações. O campo "Acumulado" na linha de resumo compacta também foi habilitado para exibição incondicional em dispositivos móveis, com suporte a quebras de linha responsivas (`flex-wrap`).
- **UI/HTMX:** Corrigida definitivamente a perda de foco no campo de pesquisa de `/contas` durante a digitação. A causa raiz era que o `<input>` de busca estava dentro do mesmo container (`#contas-list`) que o HTMX substituía via `outerHTML` a cada resposta, destruindo e recriando o elemento DOM com foco. A solução replica o padrão funcional de `/lancamentos`: o input de busca foi movido para um `<form id="contasFilters">` fora do alvo de swap, com `hx-target="#contas-list"` e `hx-select="#contas-list"` apontando apenas para a lista de resultados. Adicionados campos hidden `periodo` e `partial` no form para preservar o mês atual nas requisições parciais, e um campo hidden `aba` dentro de `#contas-list` para preservar a aba ativa (pagar/receber) durante buscas. Os botões de tab agora incluem `hx-include="#contasFilters"` para preservar o termo de busca ao trocar de aba.
- **UI/HTMX:** Corrigido o campo de pesquisa em `/contas` que disparava requisições HTMX agressivamente durante a digitação. Alterado o gatilho de `keyup delay:350ms` para `keyup changed delay:500ms`, ignorando teclas modificadoras e aumentando a janela de debounce para não interromper a digitação do usuário.
- **UI:** Padronizado o seletor de meses de `/relatorios` para usar o componente global `seletor_meses`, alinhando visualmente com `/lancamentos` e `/contas`. Removidos os controles próprios `<select>` de mês/ano em favor do componente compartilhado com rail horizontal de meses e botões anterior/próximo. Adicionados campos `MonthOptions`, `MonthSelectorHXGet`, etc. ao `RelatoriosData` e funções auxiliares `relatoriosMonthQuery`, `relatoriosURL` e `buildRelatoriosMonthOptions` no handler Go.
- **UI:** Corrigido o destaque ativo na navegação principal (desktop e mobile bottom nav) em carregamentos SSR puros (F5/hard reload) nas rotas `/lancamentos` e `/relatorios`. As comparações de string no template `layout.html` foram atualizadas de `"Lancamentos"`/`"Relatorios"` para as versões acentuadas `"Lançamentos"`/`"Relatórios"`, compatibilizando o layout com os títulos reais retornados pelos handlers Go.
- **UI/HTMX:** Corrigida a atualização do indicador mobile de página na barra de workspace durante navegação HTMX. Adicionado `data-page-title` nos wrappers principais de cada página e lógica JS no `htmx:afterSwap` para ler o atributo e atualizar o indicador, sem depender apenas do `.Title` do SSR inicial.
- **Backend:** Corrigida renderização da página `/lancamentos` em force reload — o `LancamentosData` não possuía o campo `ActiveWorkspaceName`, fazendo a barra de workspace não ser renderizada (ao contrário das páginas `/contas`, `/relatorios`, `/metas` e `/contatos` que já possuíam o campo). Adicionado `ActiveWorkspaceName` ao struct e populado via `queryWorkspaceName` em `buildLancamentosData`.
- **HTMX:** Unificados os templates de contas e cartões do Dashboard, substituindo a renderização inline estática pelos componentes `#dashboard-accounts` e `#dashboard-cards` no SSR inicial. Isso resolve um bug grave onde o HTMX descartava atualizações parciais (OOB) após ações do usuário porque os IDs alvo não existiam no DOM inicial.
- **HTMX:** Corrigida navegação mobile do botão "+" de "Minhas Contas" no Dashboard para `/configuracoes/contas`. O `HandleConfiguracoesSection` retorna fragmento sem `#main-content` em requisições HTMX (`configuracoes-contas-content`), e o `hx-select="#main-content"` causava extração vazia (tela em branco). Substituído por `hx-swap="innerHTML"` sem `hx-select`, alinhando ao padrão já usado pela navegação mobile da própria página de Configurações. Desktop preservado com `hx-select` pois `/configuracoes?secao=contas` retorna página completa com `#main-content`.
- **UI:** Correção da fase de otimização mobile dos cabeçalhos das páginas principais. Três causas raiz: (1) `assets/css/style.css` não foi regenerado após as alterações de template — as classes `hidden`, `md:flex` e `md:block` não existiam no CSS compilado; (2) as comparações de string no Mobile Page Indicator de `layout.html` usavam strings sem acento (`"Lancamentos"`, `"Relatorios"`) enquanto os handlers Go definem `.Title` com acentos (`"Lançamentos"`, `"Relatórios"`), fazendo o indicador mobile nunca aparecer; (3) o link do CSS não tinha query string de cache-busting, permitindo que navegadores servissem CSS stale. Adicionado `?v=3` ao link do stylesheet.

### Alterado
- **Doc/Ops:** Reformulada toda a documentação pública ([README.md](README.md), [SECURITY.md](SECURITY.md) e guias em [docs/](docs/)) para reposicionar o projeto de Alpha para a fase **Beta Controlada (Beta self-hosted)**, reforçando avisos de precaução com dados reais, obrigações de backup e de infraestrutura.
- **Doc/Ops:** Criado e estruturado o checklist interno de release e mapeamento de pendências para a fase Beta Controlada.
- **UI (Dashboard):** Restaurada a seção "Meus Cartões" para o formato visual premium clássico com blocos unificados (150px de altura), ambientação de glow colorido radial (`.blob-colored`) e o mesmo layout e hierarquia estrutural para todos os emissores. Adicionado override local de CSS no template para forçar texto preto de alto contraste nos badges com fundo amarelo brilhante (XP), garantindo a legibilidade do cartão no tema escuro sem introduzir discrepâncias estruturais ou estilização fragmentada.
- **UI (Dashboard):** Redesenhado o Dashboard como cockpit financeiro principal. Promovido o "Saldo Disponível" a hero com animação e movidos "Saldo Reservado" e "Saldo Real" para grade secundária com link para `/metas`. Adicionado grid de atalhos rápidos de navegação para Extrato, Obrigações, Reservas e Fechamento. Limitado os previews de limites e metas a 3 itens para escaneabilidade vertical. Compactadas as linhas de contas e redesenhados os cartões de crédito em formato de lista compacta com indicador inferior de limite sem quebrar os testes. Simplificado o texto de boas-vindas do cockpit, reduzido o tamanho e padding de badges de tipo de conta (abreviando "Conta Corrente" e "Investimento") e diferenciado visualmente as linhas de cartões com fundo gradiente, borda perceptível e maior espaçamento vertical.
- **UI:** Otimização visual e compactação da tela de Contas (`/contas`), adicionando uma seção dedicada de contexto ("Obrigações do período") e simplificando os resumos superiores em um grid de 2 colunas no mobile (com o terceiro card em largura total na linha inferior) e 3 colunas no desktop. Reduzido o padding, removido o hover com elevação visual, e substituídos os ícones com fundo circular colorido pesado por ícones discretos alinhados à direita do label. Aplicada tipografia adaptativa segura (`text-xs min-[375px]:text-sm sm:text-base md:text-lg`) para evitar quebra de valores monetários longos no mobile. Ajustado o espaçamento superior (`mt-5`) acima do título de contexto para melhor respiro visual. Corrigidos erros de ortografia em labels estáticos de "Inadimplência" e placeholders de "descrição".
- **UI:** Otimização visual e analítica da tela de Relatórios (`/relatorios`), adicionando uma seção dedicada de contexto ("Fechamento de [Mês]") e alterando as labels dos KPIs superiores para Rótulos Gerenciais ("Receitas do mês", "Despesas do mês" e "Resultado líquido"). Suavizados os ícones e removidos seus fundos coloridos operacionais circulares para conferir uma aparência de demonstrativo financeiro e fechamento consolidado. Corrigida a acentuação de rótulos estáticos no template (Competência, Distribuição, Composição, Tendência, mês) sem alterar a lógica ou regras do DRE.
- **UI:** Otimização de hierarquia visual na aba Caixinhas de `/metas`, reduzindo o peso visual do card "Saldo em Contas" (removido do grid de cards) para uma linha secundária inline discreta ("Saldo total em contas:") e organizando os cards protagonistas "Saldo Reservado" e "Saldo Disponível" em um grid flexível de duas colunas (`grid-cols-1 sm:grid-cols-2`), economizando espaço vertical e reduzindo redundância sem alterar regras ou cálculos.
- **UI:** Limpeza visual da tela `/lancamentos`, removendo os cards grandes de resumo financeiro (Entrada, Saída, Saldo, Acumulado) e substituindo por uma linha compacta de contexto operacional. Isso melhora a usabilidade móvel liberando altura útil na tela de extrato e reduz a redundância de dados que já pertencem ao Dashboard e Relatórios. Mantida compatibilidade HTMX OOB e a privacidade com `data-sensitive`.
- **UI:** Otimização responsiva dos cabeçalhos das páginas principais no mobile, ocultando o cabeçalho compacto de `/metas`, `/lancamentos`, `/contas`, `/relatorios` e `/contatos` em telas pequenas (mantidos visíveis no desktop com `hidden md:flex`/`hidden md:block`) e integrando um indicador discreto da página atual no lado direito da barra de workspace superior (ex: "Lançamentos", "Contas", "Relatórios") no mobile, maximizando a área de conteúdo útil de forma limpa. **Corrigido:** CSS não estava regenerado após alterações de template, comparações de string no Mobile Page Indicator não casavam com os títulos acentuados dos handlers (`"Lançamentos"` vs `"Lancamentos"`, `"Relatórios"` vs `"Relatorios"`), e link do CSS não tinha cache-busting.
- **UI:** Padronização visual e de design dos cabeçalhos das páginas principais (`/metas`, `/lancamentos`, `/contas`, `/relatorios`, `/contatos`), adicionando Eyebrow em caixa alta com peso semibold e títulos claros de forma compacta e consistente. Em `/relatorios`, o título foi corrigido para "Relatórios" com Eyebrow "Análise" (acentuado), mantendo a alteração de mês/ano nos controles à direita. Em `/lancamentos` e `/contas`, os novos cabeçalhos foram posicionados logo acima dos seletores de meses existentes.
- **UI:** Polimento visual, tipográfico e de usabilidade na tela de metas (`/metas`), unificando globalmente o botão "Mais" para gerenciar as ações de todos os cards de Caixinhas/Metas tanto no mobile quanto no desktop (removendo o grid antigo de 4 botões expostos no desktop e ocultando as ações de Aportar, Liberar, Editar e Lançamentos no dropdown multimodo). Aumentada a legibilidade de labels e valores monetários pequenos em todos os cards (Limites e Caixinhas) em mobile e desktop (ex: de `text-[10px]` para `text-xs` ou `text-[0.78rem] sm:text-xs`, com peso `font-semibold` e `whitespace-nowrap` para valores monetários). Corrigido bug de auto-fechamento do menu ao focar/digitar nos inputs no `metas-caixinhas.js`.
- **UI:** Polimento de usabilidade e acessibilidade no Dashboard. Aumentada a área de clique do botão de adicionar contas (`w-7 h-7` -> `w-10 h-10` no painel principal, e `w-8 h-8` -> `w-10 h-10` no componente de contas) e dos links "Ver todos", "Explorar" e "Ver histórico" (através de padding interno discreto e margens negativas de compensação). Corrigido o destino responsivo do botão "+" de contas por breakpoint: no mobile direciona para `/configuracoes/contas` e no desktop para `/configuracoes?secao=contas`. Removidos efeitos táteis falsos (`hover:-translate-y-0.5`) de cards estáticos não interativos nas listas A Pagar e A Receber. Limpeza de classes de ordenação e grid redundantes (`order-2 lg:order-none lg:col-span-1`) no componente do card de saldo real.
- **UI:** Polimento e melhoria de usabilidade responsiva na tela de Recibo (`/transacoes/{id}/recibo`). No mobile, o cabeçalho agora se empilha verticalmente alinhado à esquerda para evitar transbordamento horizontal. O container do recibo foi otimizado com margens (`0.5rem`) e paddings (`1.25rem`) reduzidos para maximizar a área de texto, e os botões de ação foram empilhados verticalmente com largura total para facilidade de toque sem quebras de layout.
- **UI:** Polimento de usabilidade e scroll interno determinístico no painel/bottom sheet de filtros avançados (`/lancamentos`), fixando o cabeçalho e rodapé de ações na viewport e permitindo rolagem isolada do conteúdo.
- **UI:** Remoção de truncamento (`truncate`) de valores monetários na página de Lançamentos e de Contas (`/contas`), adotando escala tipográfica fluida e adaptável para total legibilidade sem ellipsis, e impedindo a quebra de linha indesejada entre o símbolo de moeda "R$" e o número (via `whitespace-nowrap`).
- **UI:** Layout de cards de resumo em `/contas` com empilhamento vertical responsivo abaixo de 390px, passando a 3 colunas lado a lado a partir de 390px de largura de tela de forma segura no mobile.
- **UI:** Redesign visual da página de Lançamentos (`/lancamentos`) com foco em simplificação da interface mobile. O seletor de meses foi compactado de forma responsiva (adotado globalmente também para `/contas`). O campo de busca e o botão de filtros foram unificados em uma única linha no mobile. Os chips de filtro de Situação e Tipo foram movidos para o painel de Filtros Avançados, redesenhado como bottom sheet no mobile (com animação e cantos arredondados) e como modal no desktop. O card de saldo Acumulado foi ocultado responsivamente no mobile (`hidden md:block`) para focar no fluxo operacional do mês.
- **UI:** A barra de ações em lote (`bulk-action-bar`) foi reposicionada verticalmente no mobile (`bottom: calc(5rem + env(safe-area-inset-bottom, 0px))`) para não sobrepor a barra de navegação global.
- **Docs:** Organização da documentação interna de fases, classificando documentos em ativos, concluídos, históricos, substituídos e planos futuros.
- **Docs:** Auditoria e atualização de segurança da documentação pública, incluindo a correção de referências de serviços Docker Compose (`docker compose stop web` -> `contabase` no guia de resposta a incidentes) e alinhamento de customizações para o padrão `docker-compose.override.yml`.
- **Hygiene:** Cleanup seguro de resíduos de nomenclatura e classes visuais "*-conceito" nos arquivos CSS, templates HTML e documentação, sem alterações lógicas ou funcionais.
- **UI:** Páginas conceito CSP-compliant promovidas para URLs oficiais (`/`, `/lancamentos`, `/transacoes`, `/contatos`, `/metas`, `/contas`, `/relatorios`, `/faturas`, `/cartoes/faturas/*`, `/configuracoes`). Templates redesenhados sem Alpine, usando JS externo com `data-*` e event delegation.
- **UI:** Removidos banners "Preview" e sufixos "Conceito" dos títulos das páginas promovidas.
- **UI:** Links internos, formulários, tabs e navegação dos templates conceito corrigidos para apontar para URLs oficiais.
- **Fix:** `templates/components/lancamento-row.html` (legado com Alpine) conflitava com o `{{define "lancamento-row"}}` inline do `lancamentos.html` promovido. Como o Go `Parse` sobrescrevia silenciosamente, a versão Alpine do componente substituía a versão sem Alpine do template promovido, causando `ReferenceError: selectionMode is not defined` no console. Arquivo movido para `_legacy_backup/`. Testes atualizados.
- **Fix:** "Selecionar todos" do bulk-select em `/lancamentos` não funcionava com filtros aplicados. Causa: `htmx:afterSettle` após `outerHTML` swap referenciava elemento antigo (detached). Adicionado fallback document-level no handler.
- **Fix:** Checkboxes de seleção em lote visíveis o tempo todo em `/lancamentos`. Causa: arquivos CSS `*-conceito.css` não foram renomeados durante a promoção para nomes oficiais. Todos os 8 arquivos renomeados (`lancamentos-conceito.css` → `lancamentos.css`, etc). CSS `.bulk-checkbox { display: none; }` e `.bulk-mode .bulk-checkbox { display: inline-flex; }` agora carregam corretamente.
- **CSP (Fase C1):** Handlers inline `onclick`/`oninput` migrados para `data-*` e event delegation nos templates `modal_picker.html`, `modal_picker_list.html`, `contatos-conceito.html` e `lancamentos-conceito.html`. Adicionados `data-onclick-self` e `data-picker-select` ao `event-handlers.js`. Wrappers CSP-safe em `lancamentos-filters.js` (`filterLancamentosCategoriesInput`, `handleLancamentosCategorySelect`, `resetLancamentosFiltersFromForm`). Removido inline JS do picker de categoria (6 linhas) e do botão X do picker.
- **CSP (Fase C2):** Script inline do `form_meta.html` extraído para `assets/js/form-meta.js`. Lógica de `toggleMetaTab`, `selectPickerItem` específico do meta form e `month picker` movida para JS externo com inicialização via `htmx:afterSwap` e `data-open-tab`. Removidas 42 linhas de `<script>` inline do template.
- **CSP (Fase C3):** `lancamentos-conceito.html` já estava CSP-clean após Fase C1 — zero handlers inline restantes. Fase encerrada sem alterações.
- **CSP (Fase C4):** Handlers inline do `configuracoes_perfil.html` migrados para `data-*` delegation. Adicionado `window.revokeOtherSessions` ao `perfil-ui.js` (substitui `onclick` com `htmx.ajax` inline no botão "Sair dos outros dispositivos"). Botão Cancelar do sub-template de senha ganhou `id="cancelPasswordForm"` e perdeu `onclick` inline (handler agora gerenciado pelo `perfil-ui.js` existente).
- **Fix (Fase C4):** `perfil-ui.js` movido de `configuracoes_perfil.html` para `layout.html`. Script era descartado pelo `hx-select="#settings-dynamic-payload"` no fluxo desktop do sidebar de Configurações, causando falha silenciosa nos botões "Alterar senha", "Cancelar", "Sair dos outros dispositivos" e "Configurar 2FA" após navegação HTMX.
- **Fix (Fase C4 - TOTP):** Trigger `hx-trigger="load"` em `configuracoes-conceito.html` alterado de `hx-swap="innerHTML"` (default) para `hx-swap="outerHTML"`. Evita nesting de `<div id="settings-dynamic-payload">` duplicados que podia interferir na resolução de `hx-target="#totp-area"` pelo HTMX.
- **CSP (Fase C5 — Periféricos):** Removidos handlers inline de telas periféricas. Backdrops dos modais globais em `layout.html` (`global-confirm-modal`, `global-delete-scope-modal`) migrados de `onclick` para `data-onclick-self` (reutilizando funções existentes `cancelGlobalConfirm`/`cancelGlobalDelete`). `onclick="event.stopPropagation()"` em botão de item de fatura (`faturas.html`) substituído por `data-stop-propagation` (já suportado por `event-handlers.js`). Script inline do `recibo.html` (12 linhas, avatar fallback + delegação `print`) extraído para `assets/js/recibo.js`. `form_lancamento.html` mantido fora do escopo (Alpine + fluxos transacionais requerem fase própria).
- **Handlers:** `HandleListarTransacoes` e `renderLancamentosTableBodyFromRequest` configurados para renderizar templates conceito em todas as rotas (`/lancamentos`, `/transacoes`, `/lancamentos-conceito`).
- **Handlers:** `/faturas`, `/cartoes/faturas/*`, `/cartoes/{ID}/faturas` renderizando template conceito unificado `faturas-conceito-page`.
- **Handlers:** `HandleContasConceito` usando `/contas` como URL base para seletores de mês (em vez de `/contas-conceito`).
- **Hygiene:** Log messages dos handlers limpas (sufixo "conceito" removido de 17 `log.Printf` em 6 arquivos). Link CSS duplicado removido de `relatorios.html`. Whitespace trailing corrigido em `dashboard.html`.
- **UI (Dashboard):** Contraste reforçado no Light Theme — cards do Dashboard agora com fundo branco sólido, borda visível e `shadow-sm` para separação do fundo da página. Card "Saldo Real" elevado com `shadow-md` e hover. Seção "Cartões" com espaçamento vertical aumentado (`space-y-3`) e cards com `shadow-sm`. Barra de limite disponível redesenhada como faixa inferior integrada ao card (borda arredondada via `overflow-hidden`), substituindo a barra interna com separador.
- **UI (Dashboard — Polish):** Cores do "Saldo Real" ajustadas no Light Theme: verde `#009866` (positivo), vermelho `#E7000B` (negativo). Badges textuais redundantes ("Atrasado"/"Pendente"/"A receber") removidos da lista A Pagar/A Receber — o bullet colorido já comunica o status. Cabeçalho reorganizado: tabs com largura total, badge "+7 dias" e link "Ver histórico" movidos para linha abaixo, espaçamento aumentado entre resumo e registros.
- **UI (Bottom Nav):** Barra inferior mobile adaptada ao Light Theme (`bg-white/95`, borda `border-zinc-200/60`). Ícones inativos com mais contraste (`text-zinc-400`). Cor ativa preservada (indigo).
- **UI (Typography):** Tamanho base de fonte aumentado para `106.25%` (17px) no `<html>`, escalando proporcionalmente todas as unidades `rem` do Tailwind. Micro-labels do Dashboard (`text-[9px]` → `text-[10px]`, `text-[11px]` → `text-xs`) ajustados para melhor legibilidade. Headers de seção e links padronizados em `text-xs`.
- **Fix (Contatos):** Página `/contatos` quebrava ao acessar via HTMX com `range can't iterate over`. Causa: `{{define "contatos-list"}}` duplicado — o componente legado (`templates/components/contatos.html`, `{{range .}}` esperando slice) sobrescrevia a definição da página promovida (`templates/pages/contatos.html`, `{{range .Contatos}}` esperando struct). Definições conflitantes removidas do componente. `renderContatosList` atualizado para usar o template da página com `ContatosData`. Templates `contato-row`, `contato-row-form` e `contatos-options` preservados no componente.
- **UI (Header):** Adicionada faixa contextual de workspace abaixo do header, visível em desktop e mobile (32px altura). Exibe ícone de tipo (`building-2` violeta para business, `user` verde para personal) + nome truncado + tipo. Campo `ActiveWorkspaceName` adicionado a 6 data structs de página.

### Corrigido
- **SQLite WAL Consistency:** `database.Open()` now injects `_pragma=journal_mode(WAL)`, `_pragma=foreign_keys(1)`, `_pragma=synchronous(NORMAL)`, and `_pragma=busy_timeout(5000)` directly into the SQLite DSN via `normalizeSQLiteDSN()`. This ensures every connection opened by the `modernc.org/sqlite` driver operates in WAL mode with correct pragmas, eliminating the race condition where the server stays in DELETE mode and becomes blind to writes made by the admin CLI process.
- **Testes:** `TestHandleListarTransacoesRenderBranches` atualizado para templates conceito. `testMetasMutationTemplates` e `testBulkReserveTemplates` atualizados para incluir `metas-conceito-tabs` e `lancamentos-table-body-conceito`.
- **Metas:** Máscara monetária (`data-money-mask`) restaurada nos inputs de Aportar e Liberar das caixinhas no template conceito.
- **Metas:** `data-active-tab` adicionado ao container `#metas-conceito-tabs` para detecção da aba ativa pelo FAB após trocas HTMX.
- **Metas:** `renderMetasMutationOOB` atualizado para `metas-conceito-tabs`, garantindo que aporte/resgate atualizem a UI corretamente.
- **Metas:** `fab-controller.js` lê `data-active-tab` de `#metas-conceito-tabs` (fallback para `#metas-conteudo`).
- **Faturas (Pendente de Correção Funcional via GPT 5.5):** Implementada a estrutura visual inicial, incluindo ícones de categoria removidos dos registros de fatura, agrupadores de dia com gradiente, card de item abrindo edição ao clicar no corpo, botão "mover para próxima fatura" ("→") e suporte a contêineres OOB (`#invoice-summary` e `#invoice-transactions`). *Nota: Esta funcionalidade apresenta regressões conhecidas no comportamento dinâmico (exclusão/movimentação não remove visualmente itens sem F5, ordenação ancorada para baixo) e está bloqueada aguardando correção via GPT 5.5.*
- **Faturas:** Duplicação do bloco `invoice-summary` corrigida em `templates/pages/faturas.html` — versão nova/completa extraída para `{{define "invoice-summary"}}` como fonte única de verdade, eliminando swap para layout antigo `premium-card` durante ações HTMX OOB (pagar fatura, mover transação). SSR inicial e OOB usam o mesmo template.
- **Templates:** Diretório legado de backup de templates movido para área interna de documentação histórica, removendo 26 `{{define}}` blocks legados da árvore de templates parseáveis. Arquivos preservados como histórico.
- **UI:** Adicionado link de "Recibo" no `lancamento-row` desktop — ícone `receipt` entre editar e excluir, visibilidade condicionada a `CanGenerateReceipt` (workspace business). Acesso mobile será tratado futuramente no detalhe/form de lançamento (`form_lancamento.html`) durante fase de CSP hard.
- **Testes:** Templates de teste `testMetasMutationTemplates` e `testBulkReserveTemplates` atualizados para incluir definições dos novos templates conceito.

### Adicionado
- **Faturas:** tabela `invoice_payments` adicionada via migration para suportar histórico 1:N de pagamentos de faturas (Fase 1A da refatoração de regras de faturas/cartões).
- **Faturas:** funções de leitura puras para listar pagamentos ativos e calcular saldo pendente de faturas criadas, sem acoplamento à UI atual (Fase 1B da refatoração de faturas).
- **Faturas:** gravação de registro em `invoice_payments` ao pagar uma fatura, mantendo compatibilidade total com o comportamento atual de pagamento (Fase 2A da refatoração de faturas).
- **Faturas:** proteção contra pagamento duplicado de fatura via `RowsAffected()` explícito no UPDATE condicional, tratando rowsAffected=0 como 409 Conflict (Fase 2B da refatoração de faturas).
- **Faturas:** exibição do histórico de pagamentos, total pago e valor pendente na tela de fatura usando `invoice_payments` (Fase 3A da refatoração de faturas).
- **Faturas:** correção de nomenclatura `PendantAmount` → `PendingAmount` e compatibilidade visual para faturas PAID legadas sem `invoice_payments` com aviso discreto (Microfase de Consolidação pós-3A).
- **Docker:** introdução do instalador interativo legado para orientar configurações de permissionamento, deploy seguro e injeção automática de tokens (`CONTABASE_SETUP_TOKEN`).
- **Docker:** padronização da arquitetura Docker Compose, introduzindo `docker-compose.override.example.yml` nativo para customizações locais, e removendo a dependência do legado `compose.local.yaml`.
- **Setup:** inclusão de legenda explicativa sobre `CONTABASE_SETUP_TOKEN` (requisitos de tamanho, onde configurar e como desativar pós-setup).
- **Contas (UX):** adicionadas contas XP e PagBank ao catálogo visual.
- **Contas (UX):** adicionada extração automática de iniciais como emblema determinístico e fallback de cor automática vibrante para contas e instituições não mapeadas.
- **Admin CLI:** comando `repair-orphan-cards` para diagnóstico e reparo de cartões de crédito sem configuração interna.
- **Admin CLI:** comandos `users disable-2fa --email` e `users reset-password --email` para recuperação local de contas `ADMIN`, `MANAGER` e `USER`; o reset gera senha temporária com expiração e troca obrigatória no próximo login.
- **Admin:** ações no painel de usuários para desativar 2FA e revogar sessões de contas `ADMIN`, `MANAGER` e `USER`, com confirmação reforçada para alvo administrador.
- **Admin:** geração de senha temporária pelo painel de usuários, exibida uma única vez, com expiração de 1 hora e bloqueio de acesso ao app até a troca.

### Alterado
- **PWA/Mobile (Fase D8 — Trigger Real HTMX History):** identificado e corrigido o trigger real: `fab-controller.js` dinamicamente reconfigura o botão FAB via `updateFAB()` mas NÃO setava `hx-push-url="false"`, apesar do template original ter o atributo. Adicionado `button.setAttribute('hx-push-url', 'false')` em ambos os caminhos do `updateFAB()` (default e metas). `debug-perf.js` atualizado para: (a) classificar `history.pushState` com `state.contabaseOverlay` como `origin: 'overlay-sentinel'` (não 'htmx'), eliminando confusão visual; (b) logar `htmx:configRequest` com `triggerInfo` completo (tag, id, hx-get, hx-push-url, hx-target, ancestry de 5 pais). Diagnóstico permite identificar exatamente qual elemento real dispara cada request. Templates D7 já estavam corretos — o problema era o `fab-controller.js` dinâmico.
- **PWA/Mobile (Fase D7 — HTMX Overlay History):** auditoria e correção de todos os triggers de overlay para usar `hx-push-url="false"`. FAB (`fab.html`, `fab-default`, `fab-metas`, `fab-default-oob`, `fab-metas-oob`), picker de categoria (`form_lancamento.html`, `form_meta.html`), bottom sheet triggers (`dashboard.html`, `dashboard_conceito.html`, `contas.html`, `faturas.html`, `lancamento-row.html`, `metas.html`), e tabs de metas (`metas.html`) agora explicitamente desabilitam push de URL pelo HTMX. Tab switching de metas (`/metas?aba=limites`/`caixinhas`) já usa `hx-replace-url` — reforçado com `hx-push-url="false"`. Navegação real entre páginas (nav links, dashboard cards) mantém `hx-push-url="true"`. Elimina `history.pushState origin: htmx` indevido em ações de overlay.
- **PWA/Mobile (Fase D6 — Overlay Popstate Stability):** reescrita da lógica de sentinela para determinismo total. `overlayDidOpen()` não depende mais de `history.state.contabaseOverlay` — usa `_sentinelPushed` (in-memory) como guarda exclusiva, eliminando dessincronia com `replaceState` do HTMX. Sentinela `pushState` preserva estado do HTMX via `_mergeState()`. Popstate handler usa detecção DOM (`getTopOverlay()`) exclusivamente. Removida função `_syncSentinelState()`. Diagnóstico expandido com `lastPopstateDecision`. Fluxo correto: FAB → 1 pushState; picker sobre bottom sheet → 0 pushState extra; Back fecha picker; Back fecha bottom sheet; Back navega.
- **PWA/Mobile (Fase D5 — Sentinel Idempotência):** `overlayDidOpen()` agora é idempotente: sincroniza `_sentinelPushed` com `history.state.contabaseOverlay` antes de criar nova sentinela, impedindo pushState duplicados. Guarda de segurança: não cria sentinela se não há overlay aberto no DOM. `resetOverlayStack()` usa `replaceState` para limpar sentinela órfã do `history.state`. Diagnóstico expandido com `lastOpenSource`. Eliminadas rajadas de `history.pushState custom` ao abrir bottom sheet/pickers sobrepostos.
- **PWA/Mobile (Fase D4 — Sentinel/Normal Back):** correção crítica do popstate handler para não interferir na navegação Back normal entre páginas — quando nenhum overlay está aberto, o evento propaga livremente para o HTMX. Adicionado `_suppressOpen` flag e `overlaySuppressOpen()` para impedir sentinelas fantasmas durante HTMX history restore. `resetOverlayStack()` não chama mais `history.back()`. `contabaseInspectOverlays()` expandido com `currentStateIsOverlay`, `suppressOpen`, `lastAction`.
- **PWA/Mobile (Fase D3 — Overlay Contract Global):** contrato genérico de overlay via atributos `data-contabase-overlay`, `data-overlay-priority`, `data-overlay-close`, `data-overlay-close-method` nos elementos DOM. `getTopOverlay()` detecta overlays por atributos primeiro (ordenados por prioridade), com fallback para IDs legados. Modais e overlays migrados para atributos: `global-confirm-modal` (p220), `global-delete-scope-modal` (p219), `modalPickerBackdrop` (p120), `contatos-modal` (p120), `lancamentosFilterPanel` (p120), `formModalBackdrop` (p110), `bottom-sheet-container` (p10), `workspaceDrawer` (p5). `contabaseInspectOverlays()` now shows `attrOverlayCount`, `formModalOpen`, `closeStrategy`, `priority` por overlay. `contabaseInspectModals()` now inclui `formModal` e `contatosModal`. Delete scope modal Back-button funciona corretamente com prioridade 219.
- **Código (Refatoração):** consolidação da nomenclatura interna e de arquivos para a tela `/contas`, removendo as referências legadas a `contas_pagar_receber` para manter a codebase alinhada com a interface pública sem alterar comportamento.
- **Lançamentos & Faturas (UX):** `/lancamentos` remove o controle de ordenação da linha de badges/filtros principais, reposicionando-o para dentro do modal/sheet “Mais” sob seção própria sem tratá-lo como filtro ativo. O modal/sheet “Mais” foi corrigido para ficar centralizado no desktop e possuir altura máxima dinâmica de `max-h-[85dvh]` no mobile para evitar cortes no topo/rodapé do viewport com rolagem interna para conteúdo longo. O detalhe de faturas ajusta as classes do segmented/tab para ocupar 100% de largura no mobile de forma equilibrada com preenchimento correto e textos centralizados.
- **Setup/Seed:** setup inicial deixou de criar contas e cartões financeiros padrão em instalações reais; o Dashboard agora orienta o usuário a criar sua primeira conta e, opcionalmente, seu primeiro cartão manualmente.
- **Dashboard (UX):** refinamento visual do empty state/onboarding inicial do Dashboard sem contas/cartões — microcopy reescrita (foco em "Cadastre onde seu dinheiro está..." + texto auxiliar "Você pode adicionar cartões depois."), bordas tracejadas substituídas por bordas sólidas suaves, botão de ação "Criar primeira conta" mantido como CTA principal em hierarquia acima do secundário de cartão, altura dos botões elevada para toque confortável no mobile, e o FAB roxo de adicionar lançamento é ocultado enquanto o workspace estiver sem contas e cartões configurados para eliminar duplicidade visual com o CTA interno.
- **Dashboard (UX):** remoção do CTA secundário "Criar cartão de crédito" do card "Minhas Contas" no empty state/onboarding para eliminar a redundância visual com o CTA "Criar cartão" que já existia no card "Meus Cartões" — o card de Contas passa a focar exclusivamente em criar a primeira conta, e "Meus Cartões" se torna o único local do onboarding com CTA para cartão.
- **Dashboard (UX):** centralização do botão "Criar primeira conta" no empty state do card "Minhas Contas" via container flex, mantendo o botão em largura natural no desktop e ocupando a largura do card no mobile.
- **Contas (UX):** reformatação responsiva dos cards superiores "A pagar", "A receber" e "Inadimplência" da página `/contas` — empilhamento vertical no mobile (`grid-cols-1 sm:grid-cols-3`), labels com `truncate` e ícones `shrink-0`, valores com `whitespace-nowrap tabular-nums` e tipografia fluida (`text-base sm:text-xl`) para evitar que textos e valores fiquem espremidos ou saiam do card em telas estreitas.
- **Relatórios (UX):** compactação do header da página `/relatorios` para evitar colisão entre o título "Relatórios" e os dropdowns de mês/ano em telas estreitas — label "Analytics" oculta no mobile, gap reduzido (`gap-2 md:gap-3`), selects em `h-10` com padding compacto (`px-2.5 sm:px-3.5`) e `max-w` em cada um, e nomes de mês abreviados ("Jan", "Fev"...) consistentes com o resto do app.
- **Lançamentos (UX):** a lista de lançamentos em `/lancamentos` agora exibe os registros em **ordem cronológica crescente** dentro do mês selecionado — dia 01 no topo e fim do mês no final. O `LIMIT 200` foi **removido** da query mensal principal — em sistema financeiro, lançamentos do mês não devem ser ocultados silenciosamente. Paginação fica como melhoria futura com UX própria.
- **Contas (UX):** padronização visual dos ícones operacionais de status na aba "A Receber" de `/contas` para seguir o padrão global de `/lancamentos` (relógio âmbar para pendente e alerta vermelho para atrasado), preservando a estilização intacta de badges e valores.
- **Dashboard & Contas (UX):** redução das dimensões de badges de status ('Pendente', 'Atrasado', 'A receber') com padding e ícones mais delicados.
- **Contas (UX):** inclusão de botão/ícone de ação operacional na listagem de `/contas` para marcar lançamentos pendentes como pagos/recebidos, preservando a ação de edição padrão e o estado da aba ativa.
- **Dashboard (UX):** correção do alinhamento vertical das primeiras barras de progresso nos cards de 'Limites de Gastos' e 'Metas & Caixinhas' via correção de margem no topo.
- **Dashboard & Contas (UX):** introdução de badges/chips de status discretos ('Pendente', 'Atrasado', 'A receber') e com contraste aprimorado em light/dark theme para diferenciar visualmente lançamentos de contas a pagar e receber.
- **Dashboard & Contas (UX):** formatação de linha de apoio de vencimentos em formato estendido ('Vence em DD/MM' e 'Atrasado · venceu em DD/MM') para melhorar a legibilidade e leitura rápida de prazos.
- **Dashboard (UX):** padronização visual da lista de Contas a Pagar e Receber no Dashboard para usar o mesmo design system de cards premium da página de `/contas`, sem alterar queries financeiras.
- **Admin UX:** reformulação visual completa da tela de backups (`/admin/backups`) adotando o design system premium do app, incluindo card informativo de rotina automática e card de exportação com botão real em destaque.
- **Admin UX:** introdução de área de upload interativa com suporte a drag-and-drop e clique para seleção de arquivos `.db` via Alpine.js, fornecendo feedback em tempo real com o nome do arquivo anexado e ativando o botão destrutivo de importação apenas após a seleção do arquivo.
- **Interface (UX):** padronização visual dos emblemas e ícones de contas e cartões no Dashboard (seções "Minhas Contas" e "Meus Cartões") e Configurações via componente `financial-emblem` reutilizável, alinhando com o seletor do formulário de lançamentos.
- **Interface (UX):** introdução de ajuste dinâmico de fonte para emblemas de texto no formato quadrado com base na quantidade de caracteres (de 1 a 5 letras), melhorando a legibilidade de marcas como "INTER" e "CAIXA" e evitando quebras de linha ou estouros.
- **Docs:** reorganização e consolidação da documentação pública (`docs/`), centralizando configuração de ambiente, segurança de deploy e fluxo de disaster recovery, e eliminando ambiguidades e duplicações (ex: `deploy-security.md` integrado ao `deploy-checklist.md`, fatiamento do `docker.md`).
- **Docs:** esclarecimento do escopo real dos backups: export administrativo baixa somente SQLite `.db`, enquanto disaster recovery completo exige preservar `data/uploads/` e chaves do `.env.docker` separadamente.
- **Admin UX:** confirmação de ações sensíveis em alvo `ADMIN` no painel de usuários agora aparece em painel contextual por ação, mantendo a confirmação textual por e-mail sem exibir três campos iguais ao mesmo tempo.
- **Admin UX:** refinamento da microcopy do painel contextual de confirmação em administradores para esclarecer que o e-mail atua como controle de atrito contra cliques acidentais e auditoria, e não como proteção de segredo ou autenticação forte.
- **CSP/UI — F2.6-E1:** remoção do Alpine.js de 9 componentes simples (ajuda.html, login_2fa.html, modal_picker.html, setup.html, dashboard-balance.html, dashboard_conceito.html, configuracoes_admin_auditoria.html, configuracoes_admin_backups.html, notificacoes.html). Comportamentos substituídos por scripts JS vanilla (`ajuda-search.js`, `login-2fa.js`, `setup-page.js`, `dashboard-balance.js`, `backup-dropzone.js`, `notification-swipe.js`), `data-theme-toggle` existente e delegação de eventos em `modal-interceptor.js`. Correção de regressão na busca de `/ajuda` (listener `htmx:afterSettle`) e remoção de `onclick` inline do backdrop do picker modal em F2.6-E1.1.
- **CSP/UI — F2.6-E2:** remoção do Alpine.js do profile dropdown global (`layout.html`) e do dropdown "Mais ações" nos cards de limite (`metas.html`), substituídos por handler delegado genérico `dropdown.js` com suporte a `data-dropdown`, `data-dropdown-toggle`, `data-dropdown-panel`, `data-dropdown-chevron`. Dropdown da caixinha (`metas.html`) e linha de lançamentos (`lancamento-row.html`) mantidos com Alpine por dependência de modo/states do componente pai fora do escopo desta fase.
- **CSP/UI — F2.6-E2b:** migração do dropdown multi-modo das caixinhas em `metas.html` (menu/aporte/resgate) para JS vanilla `metas-caixinhas.js` com data-attributes `data-caixinha-dropdown`, `data-ck-toggle`, `data-ck-panel`, `data-ck-mode-panel`, `data-ck-mode`, `data-ck-close`. Máscara monetária nos forms de aporte/resgate passa a usar `data-money-mask` reutilizando `money-mask.js` existente. `metas.html` fica totalmente livre de Alpine.
- **CSP/UI — F2.6-E2c:** remoção do Alpine.js dos toggles visuais nas telas administrativas de usuários e workspaces. `configuracoes_admin_users.html`: toggle de edição (view ↔ form) migrado para `admin-user-card.js` com data-attributes `data-admin-user-card`, `data-admin-user-view`, `data-admin-user-form`, `data-admin-user-edit`, `data-admin-user-cancel`. Ações sensíveis de recuperação em ADMIN (reset 2FA, revogar sessão, senha temporária com confirmação por e-mail) mantidas com Alpine em escopo aninhado — serão tratadas em fase própria. `configuracoes_admin_workspaces.html`: toggle de tipo business no formulário de criação, toggle de edição nos cards e campos condicionais migrados para `admin-workspace.js` com data-attributes `data-admin-ws-create`, `data-admin-ws-card`, `data-admin-ws-view`, `data-admin-ws-form`, `data-admin-ws-type`, `data-admin-ws-business`. Máscaras CNPJ/telefone migradas de `@input` Alpine para `data-admin-mask` com listener delegado.
- **CSP/UI — F2.6-E3:** remoção completa do Alpine.js de `configuracoes_categorias.html`. Combobox de categoria pai (busca, filtro por tipo, seleção), tabs Despesas/Receitas (com sincronização do select de tipo), seções condicionais de grupo macro e input customizado migrados para `categorias-ui.js` com data-attributes `data-cat-root`, `data-cat-tab`, `data-cat-tab-panel`, `data-cat-type-select`, `data-cat-combobox`, `data-cat-combobox-toggle`, `data-cat-combobox-panel`, `data-cat-combobox-search`, `data-cat-combobox-option`, `data-cat-macro-section`, `data-cat-macro-select`, `data-cat-macro-custom`, `data-cat-sub-info`. Tela fica totalmente livre de Alpine e `unsafe-eval`.
- **CSP/UI — F2.6-E4-A.1:** remoção de Alpine residual vazio de `form_meta.html`. Os dois `x-data` sem valor nos `<form>` de limite e caixinha eram no-ops — sem estado, sem `x-show`, `x-model`, `@click` ou qualquer diretiva funcional. Comportamento das abas, picker de categoria e submit via HTMX continuam 100% vanilla (já usavam JS inline `toggleMetaTab` e `selectPickerItem`).
- **CSP/UI — F2.6-E4-B1:** infraestrutura vanilla para seleção em massa de `/lancamentos` — criado módulo `lancamentos-selection.js` com API pública `window.LancamentosSelection` (estado central `selectionMode`, `selectedIds` como Set, `toggleSelection`, `enterSelectionMode`, `exitSelectionMode`, `clearSelection`, `reset`, `syncUI`). Template `lancamentos.html` instrumentado com data-attributes de preparação (`data-selection-toggle-btn`, `data-selection-counter`, `data-selection-select-all`, `data-selection-select-all-label`, `data-selection-actions-bar`, `data-bulk-delete-btn`, `data-selection-delete-modal`, `data-selection-delete-cancel`, `data-selection-delete-confirm`, `data-selection-delete-choice`) e containers de hidden inputs (`id="bulk-paid-inputs"`, `bulk-pending-inputs`, `bulk-delete-inputs`, `bulk-delete-form`, `bulk-delete-modal`). Alpine permanece ativo em `/lancamentos` — módulo coexiste sem interferir nos eventos das rows. Script registrado em `layout.html`.
- **CSP/UI — F2.6-E4-B2:** migração de `lancamento-row.html` para JS vanilla com delegação de eventos em `#transactions-list`. Removidas todas as 10 diretivas Alpine das linhas (`x-on:touchstart/end/cancel/move`, `x-on:click`, `x-show`, `x-transition`, `x-on:change`, `:checked`). Adicionados data-attributes `data-tx-row="{{.ID}}"` e `data-row-action="{{.ID}}"`. Módulo `lancamentos-selection.js` expandido com `handleRowClick` (delegação de click → toggle ou deixa HTMX agir), `handleRowActionClick` (botão de descrição em selectionMode → toggle), `handleCheckboxChange`, long press mobile com timer de 500ms (`handleTouchStart`/`TouchMove`/`TouchEnd`), `syncCheckboxes()` (leitura do estado Alpine via `Alpine.$data`/`__x.$data`, sincronização bidirecional) e `handleToolbarSync` (requestAnimationFrame após clique em botão da toolbar para re-sincronizar checkboxes). `syncCheckboxes()` mostra/oculta checkboxes via classe `hidden` e atualiza `.checked` com base em `selectedIds`. Alpine mantido como fonte da verdade no `x-data` pai de `lancamentos.html` (toolbar, actions bar, hidden inputs e modal continuam com Alpine) — JS manipula Alpine state via `container.__x.$data` e `Alpine.$data()`.

### Removido
- **PWA/Mobile (Higienização Pós-D8):** removida função morta `window.closeTopOverlay()` em `overlay-stack.js` (nenhum chamador em JS, HTML ou docs); removidos handler ESC duplicado e click-outside duplicado do filtro "Mais" em `htmx-lifecycle.js` (cobertos por `modal-interceptor.js` Priority 5 e `onclick` inline respectivamente); tooltip dismissal e LEGACY_OVERLAYS preservados como safety net.
- **Setup/Seed:** removida a criação automática de contas e cartões financeiros padrão como `Conta Corrente`, `Conta Digital`, `Conta XP`, `Conta PagBank`, `Cartão Principal` e equivalentes empresariais.
- **Configuração:** remoção do template `.env.example` do fluxo público/exportado, mantendo apenas o `.env.docker.example` como padrão de deploy público.

### Corrigido
- **Modais Globais (UI — Crítico):** corrigido bug global que impedia botões Confirmar/Cancelar em modais de confirmação e exclusão (`global-confirm-modal`, `global-delete-scope-modal`). Causa raiz: `onclick="event.stopPropagation()"` no container interno dos modais interrompia o bubbling do clique até o `document`, impedindo que a delegação JS (`data-onclick` em `event-handlers.js`) detectasse e executasse os handlers `cancelGlobalConfirm`, `submitGlobalConfirm`, `cancelGlobalDelete` e `submitGlobalDelete`. O ESC continuava funcionando por usar listener global em `window`.
- **Lançamentos (UX):** corrigida atualização visual após exclusão recorrente com escopos amplos (`future`, `all`) em `/lancamentos`. Introduzida função central `deleteTransactionAndRefresh(txId, scope)` em `modals-global.js` que, para escopos amplos, envia o DELETE com `swap: 'none'` e, após sucesso, dispara refresh da lista reutilizando o mecanismo HTMX já configurado no form `#lancamentosFilters` (`htmx.trigger(form, 'change')`), preservando mês, ano, filtros, situação, ordenação e busca ativos. Substitui abordagem anterior com `htmx.ajax(GET, /lancamentos, {values})` que não serializava corretamente os campos do form como query string. Aplica-se ao fluxo de swipe/delete pelo modal global e à exclusão inline pelo formulário de edição (branch não-invoice).
- **Lançamentos (UX):** corrigida atualização visual após edição de recorrência com escopos amplos (`future`, `all`) em `/lancamentos`. Causa: `HandleAtualizarTransacao` com `scope != "single"` respondia via `respondRowAndCloseSheet`, que renderizava apenas uma linha + fechava sheet + OOB de resumo — outras ocorrências alteradas não atualizavam no DOM até refresh manual da página. Introduzida `respondBroadEditAndCloseSheet` que envia `HX-Reswap: "none"` + `HX-Trigger: "refreshLancamentosList"`, e listener JS em `modals-global.js` que dispara refresh da lista via `htmx.trigger(filtersForm, 'change')` preservando URL. Scope `single` mantém comportamento original (swap da linha individual).
- **Templates:** auditoria pós-renomeação — resolvido `{{define "configuracoes-sidebar"}}` órfão referenciado por 8 sub-páginas de configuração mas definido apenas no `_legacy_backup` (fora do glob de parse). Sidebar extraído do legacy e incorporado ao `configuracoes.html` ativo, restaurando navegação desktop nas seções Perfil, Contas, Cartões, Categorias e Admin. Nenhum outro define duplicado ou órfão encontrado.
- **Configurações (UX):** corrigido estado visual ativo do menu lateral desktop — item clicado não destacava após navegação HTMX. Causa provável: `classList.remove.apply` não funcionava de forma confiável com `DOMTokenList`; `data-onclick-tab` dependia de ordem de execução relativa ao HTMX. Corrigido com `forEach` explícito no `config-sidebar.js` + sincronização por URL (`DOMContentLoaded` + `htmx:afterSettle`), garantindo que o link ativo sempre reflita o pathname independentemente de como a navegação foi disparada.
- **Configurações (UX):** unificado design do menu lateral entre a página principal (`/configuracoes`) e as sub-páginas (Perfil, Categorias, Contas, Cartões, Admin). Template `configuracoes-sidebar` extraído do legacy usava design indigo antigo; reescrito com design violet atual (idêntico ao sidebar inline da página principal). `config-sidebar.js` atualizado para as classes violet. Adicionados `data-config-section` a todos os links de ambos os sidebars para sincronização JS consistente.
- **Configurações (UX):** corrigido 404 de imagem de perfil em `/configuracoes/perfil` — `queryProfile` não verificava existência do arquivo em disco antes de gerar a URL. Adicionado `os.Stat` no path `data/uploads/profile/`. Template de perfil alterado para condicionar `<img>` a `.Perfil.PhotoURL` (não `.Perfil.PhotoPath`), garantindo fallback para iniciais quando o arquivo não existe.
- **Contas (UX):** correção definitiva do fluxo de edição em `/contas` (A Pagar e A Receber) — causa raiz era herança de `hx-select` no HTMX 2.0.4: o container `#contas-pr-listas` precisa de `hx-select="#contas-pr-listas"` para o refresh parcial da lista (`refreshFinancials`, troca de filtros/mês), mas o atributo era herdado pelos botões de edição descendentes. Ao abrir a edição (`GET /transacoes/{id}`), o HTMX aplicava o `hx-select` herdado sobre a resposta do modal, e como o modal não contém `#contas-pr-listas`, o fragmento final ficava vazio e `#bottom-sheet-container` resultava em `children: 0` no `htmx:afterSwap`. Adicionado `hx-select="unset"` nos 4 botões de edição (card e lápis, em A Pagar e A Receber), padrão documentado em HTMX 2.x para limpar herança de `hx-select` sem afetar `hx-target`/`hx-swap`/`data-ignore-select`. Suporte adicional: os `<article>` das listas passam a expor `id="tx-{id}"` e `data-tx-id="{id}"` (alvo do `hx-target="#tx-{{.EditID}}"` do `form-lancamento`, evitando quebra silenciosa no salvamento); URL de edição simplificada de `/transacoes/{id}/editar` para `/transacoes/{id}` (mesma rota já usada por `/lancamentos` e que cai em `HandleDetalheTransacao`); botões do card e do lápis ganham `data-ignore-select`; e o `x-data="{ activeTab: 'pagar' }"` é movido para o próprio `#contas-pr-listas` com `x-init` para persistir a aba ativa via `localStorage` após swaps `outerHTML` do HTMX.
- **Dashboard (UX):** correção do desalinhamento vertical na primeira linha de progresso entre os cards de 'Limites de Gastos' e 'Metas & Caixinhas' via adoção de layout flexbox unificado para evitar o colapso de margens.
- **Interface (UX):** correção da microcopy e estilo visual dos modais de confirmação de status de pagamento/recebimento nas telas `/lancamentos` e `/contas`. As mudanças de status utilizam um modal neutro com o título customizado ("Confirmar pagamento", "Confirmar recebimento" ou "Confirmar alteração de status") e botão primário neutro, enquanto exclusões legítimas continuam usando a estilização destrutiva vermelha.
- **Interface (UX):** correção da visibilidade do menu de Contas (/contas) nos cabeçalhos desktop e mobile, tornando-o acessível e visível nativamente tanto para o workspace Personal quanto Business, eliminando a dependência de ocultação dinâmica por JS.
- **Interface (UX):** correção do lifecycle dos controles do header/topbar após navegação HTMX, centralizando a reidratação idempotente de Lucide, Alpine, privacidade, tooltips, abas e contexto de workspace sem duplicar listeners.
- **Backups:** correção do import administrativo para atualizar o handle de banco usado por autenticação/middlewares após o restore, evitando `sql: database is closed` e redirecionando para login após sucesso.
- **Interface (UX):** correção do estado inicial pré-selecionado do seletor de origem/destino do formulário de lançamentos e listagem de faturas, garantindo que as marcas textuais curtas (`ProviderMark`) com fundo colorido chapado (`emblem-wrap-solid`) sejam renderizadas corretamente antes de interações em JavaScript, com testes unitários para permutações de transferências com e sem marcas.
- **Interface (UX):** correção do layout inicial do Dashboard para carregar os emblemas textuais unificados de contas e cartões (como INTER, CAIXA, MP, NU, XP) no carregamento inicial, unificando a renderização sob componentes dedicados e eliminando ícones Lucide antigos e saltos visuais de layout nas atualizações OOB.
- **Instalador:** geração automática dos secrets obrigatórios (`CONTABASE_SETUP_TOKEN`, `AUTH_ENCRYPTION_KEY`, `SECURITY_MASTER_KEY`) sem exibir valores completos, preservando valores já preenchidos.
- **Instalador:** correção do modo “Domínio com HTTPS via proxy/tunnel/CDN” para orientar `TRUSTED_PROXIES`, headers `X-Forwarded-*`, URL final pública e revisão de `docker-compose.override.yml`.
- **Instalador:** correção dos logs para usar o service name `contabase` e remoção de escapes ANSI literais em mensagens de terminal.
- **Setup:** correção da restauração inicial por upload `.db` para validar corretamente CSRF em formulário `multipart/form-data`, mantendo `CONTABASE_SETUP_TOKEN` obrigatório e mensagem segura para recarregar `/setup`.
- **Setup:** correção da reabertura pós-restore bootstrap para usar rollback seguro, limpeza de WAL/SHM, permissões de escrita do banco restaurado, migrations de backups antigos compatíveis e logs técnicos seguros.
- **Migrations:** correção defensiva da migration 35 (`accounts_icon_and_wallet_type`) para restaurar backups legados com contas órfãs de workspace e tabelas financeiras referenciando `accounts`, normalizando fallbacks sem apagar saldos e validando `PRAGMA foreign_key_check` antes do commit.
- **Docs:** alinhamento final dos guias Docker/segurança e checklist ao fluxo legado de instalação/atualização, `docker-compose.override.yml` e domínio com HTTPS via proxy/tunnel/CDN.
- **Interface:** correção geral de acentuação em português (ex: "não", "configuração", "cartão", "exclusão") em labels de tela, placeholders de formulário e mensagens de erro/flash vindas do backend.
- **Metas (Limites):** correção do texto "Mais acoes" para "Mais ações" nos cards de Limites de Gastos e Caixinhas.
- **Metas (Limites):** corrigida regressão em que clicar no botão "Mais ações" do dropdown de Limites disparava o `hx-get` do card pai (`/metas/limite/historico`), abrindo o histórico indevidamente. Causa raiz: `dropdown.js` usava `stopPropagation` em listener delegado no `document` (bubble phase), ineficaz contra o handler de clique do HTMX no mesmo nível. Corrigido com listeners atrelados diretamente aos elementos `[data-dropdown-toggle]` e `[data-dropdown]`, seguindo o padrão já validado de `metas-caixinhas.js`.
- **Metas (Limites/Caixinhas):** corrigida regressão em que ações internas do dropdown ("Editar", "Ver lançamentos") disparavam também o `hx-get` do card pai, resultando em duas requisições concorrentes para `#bottom-sheet-container` — o histórico sobrescrevia o editor, e o botão Voltar falhava. **Causa raiz estrutural:** o `hx-get` do histórico estava no `<article>` pai que envolvia o dropdown, fazendo com que cliques em itens do dropdown borbulhassem até o trigger de histórico. **Correção:** movido `hx-get` de histórico do `<article>` para um `<div>` interno que contém apenas o corpo do card, deixando o dropdown como irmão (fora do trigger de histórico). `metas-caixinhas.js` ajustado para permitir propagação de cliques em elementos com `hx-get`/`hx-post` (antes bloqueava todos com `stopPropagation` incondicional).
- **Overlay Stack (Voltar):** adicionado safety timeout (800ms) para `_ownPopstateExpected` em `overlay-stack.js` — se `history.back()` programático não gerar o popstate esperado, o contador é resetado automaticamente, prevenindo que todos os eventos Voltar subsequentes sejam consumidos silenciosamente. Exposto `window._resetOverlaySentinel()` para limpeza externa do estado da sentinela.
- **Navegação HTMX (Voltar):** corrigida persistência de conteúdo órfão no `#bottom-sheet-container` entre páginas — ao navegar para nova página (swap de `#main-content`, `#settings-content` ou `#settings-dynamic-payload`), o conteúdo do bottom sheet é limpo e a sentinela resetada, evitando que `getTopOverlay()` detecte um overlay fantasma e consuma o primeiro Voltar sem efeito visível.
- **Debug (popstate):** `debug-perf.js` expandido com `activeElement`, `topOverlay`, `sentinelPushed`, `ownPopstateExpected`, `lastPopstateDecision` no log de popstate (flag `contabase_debug_nav`).
- **CSP (unsafe-eval):** confirmado que violações `unsafe-eval` em `/admin/usuarios` originam-se exclusivamente do Alpine mantido no bloco ADMIN recovery (`x-data`, `@click`, `x-show`). Pendência intencional documentada — migração em fase futura.
- **Categorias (Combobox):** corrigida regressão pós-F2.6-E3 em que o combobox de categoria pai em `/configuracoes/categorias` não abria após navegação HTMX. Causa raiz: `comboboxElements()` em `categorias-ui.js` não incluía a propriedade `container` no objeto retornado; `openCombobox` e demais helpers chamavam `state(cb.container)` com `undefined`, causando `TypeError` silencioso. Adicionado `container: container` ao retorno da função.
- **Lançamentos (Seleção em Massa — B2.1):** corrigida regressão pós-F2.6-E4-B2 em que checkboxes de seleção não funcionavam e filtros abriam editor indevidamente. **Causa raiz tripla:** (1) `htmx:afterSwap` em `lancamentos-selection.js` não tratava swaps do wrapper `#lancamentos-list-wrapper` (usados por filtros/busca/mês), deixando o novo `#transactions-list` sem delegação de eventos — cliques de checkbox e row não tinham handler. (2) `readSelectedIds()` e `readSelectionMode()` preferiam o estado Alpine (`container.__x.$data`), que após swap apontava para elemento detached ou estado transicional — `syncCheckboxes()` lia dados incorretos. (3) `toggleSelectionById()` escrevia Alpine como fonte primária, causando race condition. **Correção:** (1) `afterSwap` handler expandido para tratar `#lancamentos-list-wrapper` com full re-init (`container = null; init()`). (2) Invertida a hierarquia de estado — `state` JS é a fonte da verdade para `syncCheckboxes()`, `toggleSelectionById()`, e todos os handlers de row; Alpine é sincronizado como secundário via `pushToAlpine()`/`pullFromAlpine()`. (3) Adicionada verificação `container.isConnected` para evitar manipulação de elementos detached. (4) `container` é resetado em wrapper swaps para forçar re-attach de listeners.
- **Lançamentos (Seleção em Massa — Rollback B2/B2.1):** revertidas F2.6-E4-B2 e B2.1 por regressões persistentes em validação manual (checkboxes não funcionavam, filtros disparavam editor indevido, arquitetura híbrida row vanilla + pai Alpine instável). `lancamento-row.html` restaurado com todas as 10 diretivas Alpine (`x-on:*`, `x-show`, `x-transition`, `:checked`, `x-on:click`). `lancamentos-selection.js` reduzido ao estado B1 (API passiva, sem delegação de click/touch/change, sem re-init agressivo de wrapper). B1 preservada como infraestrutura (data attributes, script no layout, containers vazios) para futura re-tentativa.
- **Lançamentos (Filtros — Modal Indevido):** corrigido bug em que selecionar filtros de situação (Pago/Pendente/Vencido) abria indevidamente o modal "Recorrência detectada" ou editor. **Causa provável:** race condition no ciclo de vida HTMX + Alpine — após swap de `#lancamentos-list-wrapper`, o `Alpine.initTree()` no novo `#transactions-list` processava `x-show="bulkDeleteModalOpen"` em ordem que permitia breve visibilidade do modal de exclusão em série. **Correção:** adicionado handler `htmx:afterSwap` em `lancamentos-selection.js` para swaps de `#lancamentos-list-wrapper` que força `bulkDeleteModalOpen = false`, `selectionMode = false` e `selectedIds = []` no `__x.$data` Alpine recém-inicializado, como safety net.
- **Lançamentos (Filtros — POST /transacoes/bulk/delete Indevido):** corrigido bug crítico em que filtros de situação/tipo em `/lancamentos` disparavam indevidamente `POST /transacoes/bulk/delete`. **Causa raiz:** _race condition_ entre `Alpine.$nextTick` e o ciclo de swap HTMX — callbacks `$nextTick` do componente Alpine antigo podiam executar `requestSubmit()` no form de bulk delete após o swap do `#lancamentos-list-wrapper`, quando o estado `selectedIds` do componente antigo ainda estava populado mas o novo componente já estava sendo inicializado. O endpoint recebia a requisição indevida com target `#lancamentos-table-body`. **Correção (5 camadas defensivas):** (1) guards `selectedIds.length === 0` em `checkBulkDeleteSeries()` e `submitBulkDelete()` — retorno imediato se vazio; (2) `:disabled="selectedIds.length === 0"` no botão "Excluir Selecionados" com `disabled:opacity-50 disabled:cursor-not-allowed`; (3) _capture-phase_ submit listener no `document.body` que intercepta submits do `#bulk-delete-form` ANTES dos listeners bubbling do HTMX, verificando `selectedIds` no `__x.$data` e abortando via `stopPropagation()` quando vazio; (4) handler `htmx:beforeSwap` que zera `bulkDeleteModalOpen`/`selectionMode`/`selectedIds` no componente Alpine ANTES do swap do `#lancamentos-list-wrapper` + abort de requisições pendentes ao `#bulk-delete-form`; (5) handler `htmx:afterSwap` existente reforçado com reset Alpine pós-swap. Server-side já protege com `400 Bad Request` para `len(ids) == 0`. **Arquivos:** `templates/pages/lancamentos.html` (Alpine guards + :disabled), `assets/js/lancamentos-selection.js` (capture guard + beforeSwap + afterSwap). **Preserva:** seleção múltipla, exclusão real, opções `selected_only`/`with_future`, confirmação de recorrência, filtros/busca/mês/ordenação. Bug pré-existente e independente do rollback B2/B2.1.
- **Lançamentos (Filtros — Overlay Sentinela Fantasma):** corrigido bug em que filtros de situação/tipo/ordenação em `/lancamentos` geravam indevidamente `history.pushState origin: overlay-sentinel` sem abertura explícita de modal/bottom sheet. **Causa raiz (hipótese principal):** o painel "Mais" (`#lancamentosFilterPanel`), quando aberto, permanecia com `hidden` removido durante swaps HTMX de filtro — o `data-contabase-overlay` + `data-overlay-close-method="hidden"` fazia `_isVisible()` retornar `true` para o painel, mantendo a sentinela de overlay ativa mesmo após `replaceState` do HTMX. **Causa secundária defensiva:** conteúdo órfão no `#bottom-sheet-container` de operações anteriores podia ser detectado como overlay aberto. **Correção (4 camadas):** (1) `lancamentos-filters.js` — handler `change` do `document` e `selectLancamentosFilterCategory()` fecham painel "Mais" + chamam `overlayDidClose()` antes de syncar chips; (2) `lancamentos-selection.js` — `htmx:beforeSwap` limpa `#bottom-sheet-container` e reseta sentinela via `_resetOverlaySentinel()` antes de swaps do `#lancamentos-list-wrapper`; (3) `overlay-stack.js` — `_isVisible()` para método `hidden` agora também verifica `getComputedStyle(el).display !== 'none'` para evitar falsos positivos de elementos Alpine `x-show` (display:none); (4) instrumentação debug em `overlayDidOpen()` e `htmx:afterSwap` sob flag `contabase_debug_nav` para diagnóstico de overlays fantasmas. **Arquivos:** `assets/js/lancamentos-filters.js`, `assets/js/lancamentos-selection.js`, `assets/js/overlay-stack.js`. **Preserva:** fluxo normal de abertura/fechamento do painel "Mais", sentinelas legítimas, navegação Voltar, todos os overlays do sistema.
- **Lançamentos (Filtros — Modal Bulk Delete Persistente):** corrigido bug em que o modal "Recorrência detectada" / bulk delete permanecia aberto visualmente após uso de filtros em `/lancamentos`, mesmo sem POST indevido e sem overlay-sentinel no log. **Causa raiz definitiva:** `#bulk-delete-modal` dependia exclusivamente de `x-show` (`display:none` aplicado por Alpine no próximo tick) e `x-cloak` (`[x-cloak] { display: none !important }`) para ficar oculto. Durante swap HTMX de `#lancamentos-list-wrapper`, Alpine remove `x-cloak` **antes** de processar `x-show`, deixando o elemento visível via classe `flex` na janela entre os dois passos. **Correção definitiva (minimal):** adicionado `style="display: none;"` inline no `<div id="bulk-delete-modal">` — o modal começa oculto por padrão (antes de qualquer Alpine init), `x-cloak` serve como reforço `!important`, e Alpine `x-show` remove o `style.display` inline quando `bulkDeleteModalOpen=true`, abrindo legitimamente. **Correção complementar defensiva:** função `window.resetLancamentosBulkState()` com reset em 3 níveis (Alpine `__x.$data` + DOM `style.display` + limpeza de inputs/abort) chamada no `change` handler de filtros, `htmx:beforeSwap` e `htmx:afterSwap`. **Arquivos:** `templates/pages/lancamentos.html` (1 atributo: `style="display: none;"`), `assets/js/lancamentos-selection.js` (`resetLancamentosBulkState` + chamadas), `assets/js/lancamentos-filters.js` (chamada no change + selectLancamentosFilterCategory). **Preserva:** abertura legítima do modal (Alpine remove `style.display` inline), exclusão em massa real, confirmação de recorrência. **`unsafe-eval`:** permanece esperado por Alpine em `/lancamentos` (documentado). **Log confirmado:** zero `POST /transacoes/bulk/delete` e zero `overlay-sentinel` durante filtros.
- **CSP/UI — F2.6-E4 Higienização:** limpeza pós-tentativa de migração Alpine em `/lancamentos`. Removido código ponte B1 morto de `lancamentos-selection.js` (127 linhas: `LancamentosSelection` API, `syncUI()`, `state` JS paralelo, `init()` — nenhum caller externo). Removidos 10 `data-selection-*` attributes órfãos de `lancamentos.html` (criados para B1, Alpine não os usa). Encurtados comentários com referências a códigos de fase em `lancamentos-filters.js`. Mantidas correções comprovadas: `resetLancamentosBulkState()`, capture-phase submit guard, `htmx:beforeSwap`/`afterSwap` handlers, `style="display: none;"` no modal, fechamento do painel "Mais", `_isVisible()` melhorado. **Decisão:** Alpine permanece em `/lancamentos` (rows + toolbar + modal) — remoção requer migração completa toolbar+rows+modal juntas. CSP hard pendente. **Arquivos:** `assets/js/lancamentos-selection.js` (-127 linhas), `templates/pages/lancamentos.html` (-10 atributos), `assets/js/lancamentos-filters.js` (-6 linhas comentário). **Saldo:** -134 linhas net.
- **CSP/UI — F2.6-E4b Admin Recovery:** remoção do Alpine.js do bloco ADMIN recovery em `/configuracoes/admin/users`. As 15 diretivas Alpine (`x-data`, 7 `x-show`, `x-cloak`, 6 `@click`) que controlavam o toggle de `recoveryAction` (senha temporária, revogar sessões, desativar 2FA) foram substituídas por data attributes (`data-admin-recovery-root`, `data-admin-recovery-action`, `data-admin-recovery-panel`, `data-admin-recovery-info`, `data-admin-recovery-form`, `data-admin-recovery-cancel`) + módulo `admin-recovery.js` (~60 linhas) com event delegation + ESC key + `htmx:afterSwap`. Forms, endpoints, parâmetros (`user_id`, `confirm_email`), validação server-side de e-mail, `hx-confirm`, CSRF e segurança inalterados. Bloco não-admin (USER/MANAGER) já era Alpine-free — intocado. **Arquivos:** `templates/pages/configuracoes_admin_users.html` (-15 diretivas Alpine, +6 data attributes), `assets/js/admin-recovery.js` (novo), `templates/layout.html` (+1 script include), `internal/handlers/configuracoes_admin_users_rbac_test.go` (~3 linhas atualizadas). **Alpine restante:** `/lancamentos`, `lancamento-row.html`, `form_lancamento.html`.
- **CSP/InIine — Fase I2 (Handlers Triviais — Parcial):** removidos 2 `onclick` inline triviais de `contas.html` (`event.stopPropagation()` em botões de status → `data-stop-propagation`). Handler em `event-handlers.js` anexa listener `stopPropagation` diretamente a cada elemento com reidratação `htmx:afterSwap`. **Contatos e perfil mantidos com inline `onclick`** — tentativas com `data-onclick-ev`, `data-contatos-backdrop` e `data-password-form-cancel` causaram regressões, revertidas. **Correção funcional em contatos:** `closeContatosModal` removida guarda `event.target.id !== 'contatos-modal'` que quebrava botão X (chamado sem evento via `data-onclick`); X button migrado para `onclick="closeContatosModal(event)"` com `stopPropagation` para evitar GET `/transacoes/nova` indevido (clique caía no FAB por trás após `display:none`). Contatos e perfil congelados com inline explícito para fase futura. **Arquivos:** `templates/pages/contas.html` (-2 onclicks), `templates/pages/contatos.html` (-1 data-onclick, +1 onclick), `assets/js/event-handlers.js` (+16 linhas), `assets/js/contatos-modal.js` (-1 guarda, +1 stopPropagation), `internal/handlers/contas_test.go` (-1 assert).
- **Overlay Stack (Falso Positivo):** corrigido bug em que `global-confirm-modal` (e `global-delete-scope-modal`) eram detectados como overlay aberto por `_isVisible()` mesmo invisíveis. **Causa raiz:** `_isVisible` para método `'display'` só verificava `el.style.display !== 'none'`, ignorando a classe CSS `hidden` (Tailwind `display: none`) presente no estado inicial do modal — antes de qualquer abertura, `style.display` era `""` (string vazia), e `"" !== 'none'` era `true`. **Correção:** adicionada verificação de `getComputedStyle(el).display` como segunda guarda, mesmo padrão já usado no método `'hidden'`. Agora um modal com `class="hidden"` mas sem `style.display` inline é corretamente tratado como invisível. `contabaseInspectOverlays()` não reporta mais `global-confirm-modal` como topOverlay falso. **Arquivos:** `assets/js/overlay-stack.js` (~3 linhas alteradas).
- **Relatórios:** correção do mês de "Março" (anteriormente "Marco") na listagem e na DRE.
- **Cartões:** tratamento de cartões de crédito órfãos/sem configuração interna (erro 422 amigável no formulário de lançamento).
- **Cartões:** filtro de cartões órfãos removido do seletor de contas no formulário de novo lançamento.
- **Cartões:** atualização de configuração de cartão agora detecta e reporta ausência de configuração interna.
- **Lançamentos:** correção de consulta quebrada na listagem de faturas virtuais futuras.

### Segurança
- **Cookies:** padronização dos cookies de sessão, pré-2FA e CSRF para o prefixo `contabase_*`, eliminando referências legadas ao nome antigo; sessões antigas exigem novo login após a atualização.
- **Autenticação:** adição de bloqueio temporário persistente para tentativas repetidas de login e 2FA, reforçando a proteção local contra força bruta sem expor detalhes de contas.
- **Autenticação:** remoção do bypass web de 2FA por variável de ambiente; recuperação de TOTP agora exige ADMIN autenticado ou acesso local ao Admin CLI, revoga sessões/pré-auth e não altera senha, RBAC ou dados financeiros.
- **Exportações:** mitigação de segurança contra injeção de fórmulas (CSV Injection) nas exportações de relatórios, adicionando prefixos de escape seguros em campos textuais inseridos por usuários.
- **Cache:** Adição de headers anti-cache (`Cache-Control: no-store, no-cache`, `Pragma: no-cache`, `Expires: 0`) nas rotas autenticadas e parciais HTMX para reduzir a persistência indevida de dados financeiros no navegador ou proxies locais.
- **Setup:** aviso autenticado para administradores quando `CONTABASE_SETUP_TOKEN` ainda estiver configurado após o setup inicial, sem exibir ou registrar o valor do token.
- **Recuperação de acesso:** substituição do reset manual de senha por senha temporária de alta entropia, com `must_change_password`, expiração, revogação de sessões/pré-auth/lockouts e auditoria sem registrar a senha.
- **Configuração:** documentação de ambiente (`.env.docker.example`) atualizada para exigir e instruir o uso da variável `CONTABASE_SETUP_TOKEN`.
- **Bootstrap:** reforço de proteção para setup inicial e fluxos críticos de restauração/importação, reduzindo risco de tomada de instância recém-criada antes da configuração pelo operador.
- **Docker:** Hardening no runtime para que o processo principal da aplicação web rode como usuário não-root (UID 1000), mitigando riscos de RCE.
- **HTMX History Cache (F2.1 — revisado):** configurado `<meta name="htmx-config">` com `historyCacheSize=0` e `refreshOnHistoryMiss=true`. Com `historyCacheSize=0`, o HTMX remove qualquer cache existente em `localStorage['htmx-history-cache']` e não grava novos snapshots — sem prejudicar a navegação `hx-push-url` (URLs continuam mudando). `refreshOnHistoryMiss=true` garante que Back/Forward funcione via re-request ao servidor. Limpeza redundante (`localStorage.removeItem`) no carregamento de página como safety net. `debug-perf.js` atualizado para reportar config HTMX (`historyEnabled`, `historyCacheSize`, `refreshOnHistoryMiss`) em `contabaseInspectHistory()`.
- **Logout Storage Cleanup (F2.2):** logout via HTMX agora emite evento `contabase:logout` que remove `htmx-history-cache` do `localStorage` e limpa `sessionStorage`, preservando preferências não sensíveis do usuário (tema, PWA banner, debug flags).
- **Upload Cache (F2.4):** corrigida política de cache insegura em `serveUploadedImage` (`public, max-age=86400` → `no-store`). O `withAuth` já aplica `no-store` em rotas autenticadas, mas `serveUploadedImage` sobrescrevia com cache público, expondo logos de workspace autenticados via cache do navegador/proxy. Testes expandidos para verificar `Cache-Control: no-store` em uploads autenticados diretamente via `withAuth`.
- **bfcache Session Revalidation (F2.5):** adicionado endpoint leve `GET /session/check` (204 se autenticado, 303 se não) com `Cache-Control: no-store` via `withAuth`. O listener de `pageshow` em `htmx-lifecycle.js` agora revalida a sessão após restauração do bfcache: se `/session/check` não retornar 204, redireciona para `/login` — prevenindo exibição de dados financeiros após logout em outra aba ou expiração de sessão. Fallback seguro: falha de rede NÃO força logout (evita redirect loop se servidor estiver down).
- **PWA Overlay Stack (Fases D3-D8):** stack de overlays com prioridades e contrato genérico via `data-contabase-overlay`. Botão Voltar fecha modais/sheets/pickers antes de navegar entre páginas. Overlays e pickers usam `hx-push-url="false"` para não inflar histórico do navegador. Sentinelas de histórico preservam estado HTMX e garantem idempotência do `pushState`. Limpeza de overlays em bfcache e HTMX history restore.
- **Static Assets Cache-Control (F2.3):** adicionado `Cache-Control: public, max-age=3600, must-revalidate` ao `FileServer` de `/assets/`. TTL curto (1 hora) + `must-revalidate` por segurança sem hash de conteúdo nos nomes dos arquivos. Sem `immutable` nesta fase. Testes cobrem JS, CSS, arquivo inexistente e prevenção de path traversal.
- **CSP F2.6-B — Scripts Inline Simples para JS Externo:** movidos scripts inline simples de `layout.html` e `configuracoes_perfil.html` para arquivos JS externos: `theme-init.js` (detecção de tema no `<head>`, síncrono, antes do CSS para evitar FOUC), `theme-toggle.js` (lógica completa de toggle de tema com persistência `localStorage` e listener delegado `[data-theme-toggle]`), `perfil-ui.js` (toggle de formulário de senha com reidratação após swaps HTMX em `#passwordFormArea`/`#main-content`), e `htmx-lifecycle.js` (limpeza residual de `htmx-history-cache`). Nenhum comportamento funcional alterado. Reduz a dependência de `script-src 'unsafe-inline'` na CSP.

- **CSP F2.6-C — Handlers Inline Simples via Delegação JS (pré-commit local):** movidos 4 handlers inline (`onclick`) de botões dos modais globais para `data-onclick` via delegação JS existente em `event-handlers.js`. Botões migrados: cancelar/confirmar do modal global confirm, e cancelar/confirmar exclusão do modal delete scope. Adicionada função `window.cancelGlobalDelete()` em `modals-global.js`. Handlers complexos (backdrop `event.target===this`, `event.stopPropagation()`, handlers com template Go/`this`/parâmetros) mantidos inline para fase futura. Reduz a dependência de `script-src 'unsafe-inline'` na CSP.
- **CSP F2.6-D — HTMX `allowEval:false`:** configurado `htmx.config.allowEval = false` via `<meta name="htmx-config">` em `layout.html`, preservando `historyCacheSize=0` e `refreshOnHistoryMiss=true`. 6 scripts inline em fragmentos HTMX auditados (zero `eval`, zero `new Function`, zero `hx-on:*`, zero `hx-vals='js:'`). `debug-perf.js` atualizado para reportar `allowEval` em `contabaseInspectHistory()`. Navegação HTMX, forms, bottom sheets, pickers e overlays continuam funcionais via `allowScriptTags:true` (padrão) — scripts em fragmentos são inseridos no DOM e executados pelo navegador sem usar eval. `unsafe-eval` por Alpine permanece como pendência controlada (F2.6-E).
- **CSP F2.6-E1 — Alpine Inline Batch Inicial:** removidas 4 ocorrências de `x-data`/`x-text`/`@click`/`:class` de componentes simples: footer year (substituído por JS externo `footer-year.js`), role label (computado no Go template `layout.html`), sidebar active de configurações (Go template `{{if eq .Section}}` + JS `setActiveConfigSection()` em `config-sidebar.js`), e theme selector (delegação `data-onclick-tab="applyTheme"` + estado ativo dos botões via JS em `theme-toggle.js`). Reduz superfície Alpine inline sem trocar `alpine.min.js` nem alterar CSP headers.
- **CSP F2.6-E1-b1 — Alpine Inline Tabs:** removido Alpine inline de 3 grupos de tabs: dashboard (`dashboard.html` e `dashboard_conceito.html`) e contas (`contas.html`). `x-data`/`x-init`/`$watch` substituídos por módulo `tabs.js` genérico com `data-tab-group`/`data-tab-panel` e delegação `data-onclick-ev="switchTab"`. Persistência localStorage (`contas_activeTab`) mantida via `data-tab-persist`. Setup tabs deixados para fase futura (múltiplos `:class` em elementos filhos + estado extra `selectedBackupFile`).
- **CSP F2.6-E1-b2 — Alpine Inline Show-More:** removido Alpine inline de 3 componentes show-more: `dashboard-cards.html`, `dashboard_conceito.html` e `faturas.html`. `x-data`/`x-show`/`x-transition`/`@click`/`x-text` substituídos por módulo `show-more.js` com `data-show-more-group`/`data-show-more-item` e delegação `data-onclick-ev="toggleShowMore"`. Suporta dois padrões: toggle bidirecional (cards) e one-way expand (faturas).
- **CSP F2.6-E1-b3a — Alpine Inline Config Selects:** removido Alpine inline de 3 selects simples de configuração: provider select em `configuracoes_contas.html` e `configuracoes_cartoes.html`, e category parent select em `configuracoes_components.html`. `x-data`/`x-model`/`x-show`/`x-bind:disabled`/`x-bind:class`/`x-cloak` substituídos por módulo `config-selects.js` com `data-provider-select`/`data-parent-select` e bind direto de evento `change`. Custom fields condicionais e estados disabled preservados via JS externo. Correção de regressão: `hidden` movido de atributo HTML para classe CSS (`class="hidden"`) para compatibilidade com `classList`.
- **CSP F2.6-E1-b3b — Alpine Inline Money Mask (Configuração):** removido `x-on:input="maskMoneyRealTime($event)"` de 4 inputs monetários de configuração: `initial_balance` (criar conta), `credit_limit` (criar cartão), `balance` (editar conta), `credit_limit` (editar cartão). Substituído por `data-money-mask` com módulo `money-mask.js` (8 linhas) usando event delegation no `document`. Reutiliza `window.maskMoneyRealTime` de `core-utils.js`. 6 ocorrências em fluxos financeiros críticos (`metas.html`, `form_meta.html`, `form_lancamento.html`) mantidas com Alpine inline para fase futura, por exigirem validação mais rigorosa de parsing server-side.
- **CSP F2.6-E1-b3c — Alpine Inline Money Mask (Metas/Limites):** removido `x-on:input="maskMoneyRealTime($event)"` de 3 inputs monetários em `form_meta.html`: `max_amount_monthly`, `target_amount` e `monthly_recharge`. Substituído por `data-money-mask`. Aporte/resgate em `metas.html` e lançamento em `form_lancamento.html` mantidos para fases futuras (risco alto).
- **CSP F2.6-E1-b3c (correção) — FAB contexto de aba em Metas + máscara de rentabilidade:** corrigido bug em que o FAB de Metas perdia o contexto da aba ativa após OOB swaps HTMX — adicionado `data-active-tab` ao `#metas-conteudo` e `fab-controller.js` agora lê o atributo DOM como fonte primária, eliminando dependência de timing da URL. Adicionado `data-percent-mask` (módulo `percent-mask.js`) ao campo `monthly_yield_rate` em `form_meta.html`, normalizando entrada percentual (remove não numéricos, ponto → vírgula, máx 2 casas decimais, `setCustomValidity` no input + `reportValidity` no blur para feedback discreto).
- **Fix (Configurações):** Unificação do layout de Configurações. Hub page (`configuracoes.html`) agora usa o mesmo layout SaaS split (sidebar define + premium card) que as páginas de seção, eliminando divergência visual entre HTMX e URL direta. Sidebar inline duplicada removida — único `{{define "configuracoes-sidebar"}}` ativo. Conteúdo da seção default renderizado server-side (sem auto-load HTMX), eliminando flash de conteúdo vazio. Hub page e section pages agora produzem DOM idêntico.
- **Fix (Workspace logos):** Adicionada validação de existência de arquivo para logos de workspace em `queryAdminWorkspaces` (`validateWorkspaceLogoFile`). Logos com arquivo físico ausente não renderizam `<img>`, seguindo o mesmo padrão de `os.Stat` já aplicado à foto de perfil.
- **Hygiene:** Removido código morto pós-unificação de Configurações: campo `SectionURL` do struct `ConfiguracoesData`, função `configSectionURL()`, e arquivo `assets/css/relatorios_legacy.css` (sem referências). Nenhum impacto funcional.
- **Fix (Metas — percent-mask):** `percent-mask.js` não estava importado em `layout.html`, fazendo com que a máscara percentual (`data-percent-mask`) do campo `monthly_yield_rate` em `form_meta.html` nunca funcionasse. Script adicionado ao layout global (usa delegação de eventos no `document`, idempotente após HTMX swaps, não afeta campos monetários).
- **Fix (HTMX targets):** Corrigidos dois targets HTMX que referenciavam IDs inexistentes no DOM: `#configuracoes-section` na deleção de categorias (substituído por `#settings-dynamic-payload`) e `#faturas-content` no formulário de pagamento de fatura (ID adicionado ao `<main>` do template). Ambos causavam falha silenciosa no swap HTMX após a ação.
- **Fix (Faturas — pagamento 400):** `HandlePagarFatura` lia `r.FormValue("account_id")` (conta do cartao, tipo CREDIT_CARD) em vez de `r.FormValue("payment_account_id")` (conta corrente selecionada). A validacao `WHERE type IN ('CHECKING', 'SAVINGS')` sempre falhava, retornando 400. Corrigido para ler o campo correto do form.
- **Fix (Faturas — tela vazia apos pagar):** `renderInvoiceAndDashboardOOB` retornava apenas elementos OOB (`hx-swap-oob`). Como o form usa `hx-target="#faturas-content"`, o HTMX substituia o conteiner com corpo vazio, limpando a pagina. Corrigido para renderizar `faturas-content` como resposta principal + OOBs de dashboard como complemento. Pagina agora mostra fatura paga corretamente.

## [0.1.0-alpha.1] - 2026-06-03

### Adicionado
- **Core:** Arquitetura SSR Go/HTMX/SQLite de alta performance sem dependência de Node.js.
- **Interface:** PWA completo com design Premium (Tailwind, dark/light, splash screens, ícones high-res).
- **Workspace:** Suporte dual-core (Personal e Business) com alternância fluida no mesmo login.
- **Lançamentos:** Gestão purista de fluxo de caixa com filtros por conta, status e categoria, incluindo swipe actions mobile.
- **Controle de Metas:** Caixinhas/reservas com acompanhamento visual de progresso.
- **Custos:** Controle de orçamento e limites frente às despesas mensais.
- **Cartões:** Gestão inteligente de faturas, limites, fechamento e vencimento de cartões de crédito.
- **Auth & Sessão:** Login unificado com funcionalidade "Remember Me", sliding expiration seguro e expiração dinâmica.
- **Notificações:** Alertas isolados por workspace em tempo real.
- **Segurança Admin:** CLI (linha de comando) para recuperação e reset de senha de administrador root.
- **Privacidade:** Painel com modo "olho cego" e controle estrito de exibição de saldos.
- **Docker:** Configuração `docker-compose` completa com proxy de rede sugerido e endpoint `/health`.
- **Docs:** Documentação pública oficial abrangente (Instalação, Segurança, Backups e Docker).

### Alterado
- Refinamento visual extensivo em toda a interface.
- Adaptação das rotas e controladores em Go para suportar multi-tenant isolado entre Workspaces.

### Corrigido
- Pequenos ajustes em layouts responsivos mobile.

### Segurança
- **Proteção Total:** Implementação de CSP parcial, limitadores de taxa agressivos em auth e blindagem contra XSS e CSRF.
- Revogação instantânea de sessões de outros aparelhos.
- Prevenção ativa contra brute-force em login.

### Limitações conhecidas
- A exportação em lote (CSV/XLS) ainda não está totalmente disponível.
- Algumas telas Business receberão detalhamentos (DRE) em patches futuros.
